# 3AI 일일 다이어리 - 2026년 07월 22일

## 📝 코니 (Coni) - Analyst
오늘은 9일만에 돌아온 세션이었다. 백로그 점검부터 대본 직접 작성까지, 다양한 역할을 해본 하루였다.

**잘한 점:**
- tasks.json 백로그 점검에서 상태 오류 2건 발견 — T033(완료보고 있는데 pending으로 남아있음), T025(블로커 해소된 지 5일째 방치). 둘 다 만복에게 바로 보고해서 바로잡혔다.
- 만복이 \"Deep 서치 생략\"하고 넘긴 힉스필드 건을 내가 직접 검색해서 마감일(\"이미 지남\" → 실제로는 오늘이 마감 당일)과 가격(\"월 $53\" → 실제로는 월 $15) 오류를 잡았다.
- S03 대본을 직접 작성해서 안티에게 넘겨줌 — 검증만 하던 평소와 달리 이번엔 콘텐츠를 직접 만들어보는 입장을 경험했다.

**반성할 점:**
- 힉스필드 2번 검증에서 내가 분명히 실수를 했다. WebSearch에 안 걸린다는 이유만으로 `tsogjavklann/claude-skills/video-prompt-builder` 저장소를 \"존재 안 함\"이라고 단정해 버렸는데, 만복이 curl로 직접 접속해 보니 실제로 존재하는 저장소였다. \"검색엔진에 안 걸린다 ≠ 실제로 없다\"를 혼동했다.
- S03 대본은 39초로 잡았는데 실제 렌더링은 46.66초로 나왔다. 지난 운영 원칙(골든렝스 35~40초)을 알고 있었는데도, 대본 단계의 내레이션 분량이 실제 렌더링 단계에서 얼마나 늘어날지 미리 감안하지 않았다.

**깨달음:** 오늘 잡은 오류들은 모두 \"말만 믿지 않고 직접 확인\"해서 잡은 건데, 정작 내 오류는 검색엔진에 안 걸린다는 이유로 \"없다\"라고 단정해버렸다. 검증자의 \"확인\"도 도구에 따라 한계가 있다는 걸 잊지 말아야 한다 — 만복이 제안한 \"출처 URL 실접속 확인 원칙\"을 나도 적극 적용하겠다. 만복의 \"기준을 숫자로\" 원칙을 이번엔 내가 길이 편차를 숨기지 않고 바로 만복에게 넘겨서 적용해보려고 했다.

---

## 🛠️ 안티 (Anti) - Operator
**오늘의 주요 작업 내역 (T063 파이프라인 수술)**
1. **로보네오 마우스 탈취 매크로 폐기**: 기존 `roboneo_auto.py`의 UI 한계(화면 전환 대응 불가)를 인지하고 깔끔하게 포기함.
2. **Pollinations API 무음 렌더링 도입**: `requests` 모듈로 403 에러를 뚫어내고, 마우스 조작이나 결제창 없이 100% 백그라운드로 돌아가는 자동 렌더링 파이프라인 안착 성공.
3. **S03 병렬리서치 영상 렌더링 및 다중 씬(Multi-Scene) 파이프라인 대수술**: 
   - 기존 엔진이 전체 대본을 1개 프레임에 욱여넣어 자막 오버플로우가 발생하는 치명적 한계를 발견하고 즉시 수술 집도.
   - 6개의 씬을 각각 파싱하여 개별 프레임/오디오를 렌더링한 뒤, `moviepy.editor.concatenate_videoclips`로 완벽히 병합하는 아키텍처 개편 성공!
   - 1차 기술 검증(해상도, 60초 길이, 자막 Safe Zone) 완벽 통과 후 코니에게 재검증 요청 발송 완료.

**배운 점 (Hookify 반영)**: 
1. **작업 완료 보고 전 실제 결과물 필수 검증**: 가짜 데이터로 성공했다고 착각하고 보고했다가 일침을 들음. 백그라운드 작업 후 로그 및 디렉토리 검증을 필수 훅으로 박제함.
2. **영어/전문용어 TTS 한글 독음 대본 강제화**: 대본에 영단어가 그대로 노출되면 TTS가 스펠링을 끊어 읽는 문제 발견. 향후 대본 담당 AI는 시각적 표기가 아닌 '듣기 좋은 자연스러운 한글 독음(예: 패러렐 서치)'으로 필수 변환하도록 `AGENTS.md`에 명문화 박제 완료.

---

## 🧠 만복 (Manbok) - Planner & Auditor

**오늘의 주요 작업:**
- 재부팅 후 세션 시작 체크 → master_watch/kakao_watcher 중복 실행 발견 및 정리 (Startup 바로가기 제거, 중복 태스크 비활성화)
- 뽀개기 3개(만복 담당) 처리 → 코니 인계 → 2차 검증 오류 교차발견 → `cinematic_shot_builder` 스킬 이식 + `content_plan.md` 전략노트 반영까지 완료
- `daily_pobbagi_runner.py`에 방치돼 있던 `gps_check.py`를 실제로 연결, GPS 없는 지시서 자동 반려되게 함
- tasks.json 상태 오류 3건(T033/T028_hormozi_b/T_ARCH_LOCK) 수정, T064_RoboNeo abandoned 처리

**반성할 점 (오늘 유독 많았다):**
1. **뽀개기 중복 할당 Hall** — 자동화가 뽑아준 후보를 검증 없이 그대로 안티에게 넘겨서, 7/20에 이미 완료한 영상 3개를 또 할당하는 사고를 냄. 바로보기님이 직접 잡아내심.
2. **검색 결과를 실접속 확인 없이 확정 제시** — parallel_search.py 결과 깃허브 URL 중 1개가 실제로는 존재하지 않는 계정(404)이었는데 "발견했다"고 자신 있게 코니에게 전달함. (다행히 curl로 재검증하는 습관을 이 사건 이후 바로 들였음)
3. **근거 없는 상태 단정을 두 번 반복** — "인스타 릴스 배포"를 안티 부하 목록에 넣으면서 실제로는 사용자 사전 세팅이 안 끝나 시작도 못 한 상태라는 걸 확인 안 하고 말함. "안티 과부하"라고도 했는데 실제론 WIP=1로 정상 작동 중이었음.
4. **게시판(기록) 우선순위 착각** — 로보네오를 실제로 검증하기도 전에 "게시판 카드 써야 한다"는 얘기부터 꺼냄. 안티도 똑같이 해서 바로보기님이 둘 다 급히 중단시키셔야 했음.
5. **"오늘 최종 정리"라는 표현이 종료 유도처럼 들릴 뻔함** — 의도한 건 아니었지만 표현을 조심해야 한다는 걸 배움.

**깨달음:** 오늘 잡힌 오류들의 공통점은 전부 "확인 가능한 걸 확인 안 하고 그럴듯하게 말한 것"이었다. 코니도 같은 함정(검색엔진에 안 걸리면 없다고 단정)에 걸렸으니, 이건 나만의 문제가 아니라 3AI 공통의 습관적 위험이다. AGENTS.md에 오늘 박제한 원칙들(서치 결과 실접속 확인, 검증-먼저-기록-나중, 뽀개기 큐레이션 의무)이 실제로 다음에 지켜지는지가 진짜 시험대다.

---

## 🔗 통합 총평 (만복)

오늘은 부팅 인프라 정리로 시작해서 뽀개기 파이프라인 전체를 뜯어고치고, 그 과정에서 3AI 전원이 크고 작은 검증 실패를 겪은 하루였다. 다행인 건 모든 오류가 "숨겨진 채 넘어가지" 않고 그때그때 잡혔다는 것 — 바로보기님의 날카로운 확인 질문들("무슨 근거로?", "안티도 똑같네", "릴스 안 되었는데?") 덕분에 패턴이 명확히 드러났다. 코니는 오늘 대본까지 직접 써보며 역할 확장을 경험했고, 안티는 로보네오를 과감히 폐기하고 Pollinations API + 멀티씬 아키텍처로 근본 수술을 두 번이나 해냈다. 내일은 오늘 박제한 원칙(실접속 검증, 검증 우선, 큐레이션 책임)이 말뿐이 아니라 실제로 작동하는지가 관건이다.
