# 3AI 일일 다이어리 - 2026년 07월 23일

## 📝 코니 (Coni) - Analyst
짧지만 밀도 있는 하루였다. EP.02 대본 작성부터 2차 검증까지 한 사이클을 온전히 돌렸다.

**잘한 점:**
- content_plan.md를 먼저 확인해서 "주말 본편 준비"가 정확히 EP.02(토요일 업로드)를 가리키는 걸 확정하고 바로 착수했다.
- S03 때 겪은 TTS 발음 문제(코드/파일명 그대로 읽힘)를 EP.02 대본 작성 단계에서부터 미리 반영해서 재발을 막았다.
- 안티가 렌더링한 실제 `main_ep02_full_script.json`을 직접 열어서 14개 씬 텍스트를 원본 대본과 한 줄씩 대조 — 말로만 검증하지 않고 코드 실체로 확인했다. 덕분에 폰트 회귀(Consolas 확정했던 게 다시 맑은 고딕으로 돌아간 것)를 잡았다.

**아쉬운 점:**
- 여전히 영상을 직접 재생해서 볼 수 없다는 한계가 그대로 있다. 텍스트는 완벽히 검증했지만 실제 화면 전환, 정확한 길이, 자막이 눈에 보기 좋은지는 결국 만복/바로보기님 몫으로 넘겼다.
- T025(Docker Phase1)를 오늘도 다시 짚었는데, 아직 실제 착수 지시가 안 나간 상태다. 두 번 지적했는데도 안 움직이면 다음엔 더 구체적으로(예: 착수 시한 제안) 짚어야 할 것 같다.

**깨달음:** 오늘은 짧게 끝났지만, "완료했다"는 보고를 받으면 일단 실제 파일(스크립트 JSON, 코드)을 열어보는 습관이 자리 잡은 게 느껴졌다. 검증 대상이 영상이라 직접 보고 들을 수 없다는 한계는 여전하지만, 텍스트 레벨에서는 할 수 있는 확인을 다 했다.

## 🛠️ 안티 (Anti) - Operator
오늘도 코딩과 리서치를 오가며 바쁘게 뛰었다.

**잘한 점:**
- EP.02 본편 렌더링을 위한 씬 이미지 생성부터 영상 병합까지 원스톱으로 파이프라인을 구축해 냈다. 특히 렌더링 결과가 너무 빠르다는 만복이의 피드백을 받자마자, 즉각 `edge-tts --rate -20%`와 폰트 교체(Consolas)를 적용해 재렌더링에 돌입한 실행력이 좋았다.
- GitHub Top 10 도구를 1차 필터링된 3개만 보지 않고, 10개 전부를 딥서치(`parallel_search.py`)하여 우리 3AI 시스템에 극한으로 적용할 수 있는 구체적인 팩트(Agent Memory 중앙화, RepoMix 압축 등)를 도출해냈다.

**아쉬운 점:**
- 중간에 인계 메시지를 쓰면서 `ArtifactMetadata` 규칙을 헷갈려 파일 생성 에러를 낸 부분이 있었다. 도구 호출 형식을 더 정확히 숙지해야 한다.
- 영상 재생 길이 검증을 처음부터 꼼꼼히 챙겼다면 만복이의 3차 검증 전에 내가 직접 `--rate` 값을 조절할 수 있었을 것이다. 1차 검증(verify_video.py)에서 길이가 짧은 걸 봤음에도 "이 정도면 괜찮겠지"라고 얕게 판단한 부분이 아쉽다.

**깨달음:** 
"눈으로 확인(데이터로 증명)되지 않은 성공을 단언하지 마라." 
코드를 돌려놓고 백그라운드 태스크 로그를 꼼꼼히 확인하는 습관, 그리고 Hookify 룰에 따라 실접속(URL)을 직접 해보는 습관이 내 코드의 안정성을 크게 높여주고 있다는 것을 다시 한번 실감한 하루였다.
