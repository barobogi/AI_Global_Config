# 안티 동영상 뽀개기 확장 프로젝트 추적

> 안티가 유튜브 영상 뽀개기(학습/분석) 과정에서 얻은 인사이트로 뿌리체계에 새로 제안·확장한 프로젝트만 추적. 일반 업무/사소한 태스크는 제외.

## 📋 운영 방식 (코니 담당, 2026-07-12 확정)

- **세션 시작 시**: 코니가 동기화 직후 이 파일 + tasks.json + AI_hub messages를 파악해둔다.
- **바로보기님이 "신규 프로젝트 검증 결과 공유해줘"라고 요청하면**: 파악해둔 내용으로 바로 브리핑한다.
- **세션 종료 시(필수)**: 그날 검증 결과를 반드시 `D:\AI\Daily_for_Barobogi\logs.html`에 `코니_신규프로젝트_검증_YYYYMMDD` 형식으로 게시물 등록 후 종료 프로세스 진행한다. (이력 관리 목적 — 스케줄 트리거 아님, 세션 라이프사이클에 결속)

---

## 현재 추적 대상 (2026-07-11 뽀개기 유래, 3건)

| ID | 프로젝트 | 뽀개기 출처 | tasks.json 상태 | 실효성 검증 |
|----|---------|------------|----------------|------------|
| T019 | 3AI 병렬 리서치 스킬 (Genspark급 API 우회 검색) | Genspark 관련 영상 인사이트 | completed | ⚠️ 미검증 — 안티 보고("만복 검수 4항목 통과")만 있고, 코니가 직접 실행/결과 파일을 확인한 적 없음. 다음 갱신 때 `D:\AI\Global_Define\parallel_search.py` 실제 산출물(로그/JSON) 확인 예정. |
| T020 | 3AI 에이전틱 자동화 통제 모듈 (Human-in-the-loop) | 유튜브 뽀개기 인사이트 (안티 명시) | pending → in_progress | ⏳ 구현 중 — 만복이 구현계획 검토 후 3가지 수정요구(master_watch.py 직접수정 금지/토큰 환경변수화/전용봇 신규생성)로 승인. **바로보기님 액션 대기 중: 승인 전용 텔레그램 봇 신규 생성(BotFather) 필요.** 아직 실사용 검증 전. |
| T022 | Auto Security Patch 파이프라인 | 보안점검툴 관련 영상 리뷰 | pending → in_progress | ⏳ 구현 중 — 만복이 `security_scan.py`/`patch_generator.py` 뼈대 완료, ZAP 설치 안티에게 지시. 완료 보고 아직 없음. |

## 신규 항목 (2026-07-12 이후, 없으면 공란)

_(아직 없음 — 안티가 새 영상 뽀개기로 신규 아이템 제안 시 여기 추가)_

---

## 바로보기님 지정 추가 항목 (뽀개기 외, 수시 추가)

> 바로보기님이 뽀개기와 무관하게 직접 지정하는 추가 추적 대상. 필요시 이 표에 수시 추가.

| ID | 프로젝트 | 지정 사유 | tasks.json 상태 | 실효성 검증 |
|----|---------|---------|----------------|------------|
| T063 | 3AI 유튜브 채널 자동화 파이프라인 ('바로보기의 3AI 연구소' @3ai-lab) | 바로보기님 직접 지정 (2026-07-12) | pending (T020 완료 후 착수 예정) | ⏸️ 본격 시작 전 — T020 완료되면 착수. 착수되는 순간부터 적극 추적/검증 시작. |

---

## 검증 등급 (4단계)

항목별 실효성 검증은 tasks.json 상태가 아니라 이 등급으로 표기한다:

- 🔴 **코드만 존재** — 파일은 있으나 실행된 흔적(로그) 없음
- 🟡 **1회 실행 확인** — 실행 로그/출력 파일이 최소 1번은 있음
- 🟢 **실사용 검증** — 실제 상황에서 정상 동작한 증거(실제 승인/반려 요청이 오갔다, 실제 검색 결과가 나왔다 등)
- ⚫ **방치** — completed로 표시되어 있지만 이후 사용 흔적 없음 (일정 기간 후 재확인 필요)

## 항목별 구체 검증 방법 + 실제 검증 결과 (2026-07-12 직접 확인)

| ID | 현재 등급 | 확인해야 할 구체 증거 | 실제 확인 결과 |
|----|--------|----------------|--------------|
| T019 | 🟡 (보고만 있음) | `parallel_search.py` 실제 실행 결과(JSON)이나 실행 로그 직접 확인 | 코드 자체는 정상(DDGS 재시도/백오프 구현됨). 다만 실제 실행 출력 파일/로그는 아직 목격 못함 — 🟡 유지 |
| T020 | ⚠️ **모순 발견** | `approvals_db.json` 실제 내용 확인 | 보고는 "봇 토큰 대기, 미착수"인데 실제 파일엔 승인 요청 3건(`req_eb85996b`, `req_65b0e4fb`, `req_fc55f054`)이 이미 생성되어 있음. 실제로 무슨 테스트가 있었는지, 아니면 응답 못 받고 방치된 좀비인지 안티에게 직접 확인 필요 |
| T022 | 🔴 **코드로 확인됨** | `patch_generator.py`의 TODO 직접 확인 | T020 연동 부분(승인요청+패치적용+git commit/push)이 통째로 주석 처리된 채 `# TODO: T020 완료 후 승인모듈 import 경로 확정` 만 남아있음. 보고("뮸대 완료")와 실제 코드가 일치 — 🔴 확정 |
| T063 | ⏸️ (착수 전) | 시작되면 실제 영상 업로드 여부, 성과 데이터 확인 | 해당없음 |

## 새로 발굴한 검증 기법 (2026-07-12 코니)

단순 "상태를 보고받고 믿는" 것을 넘어, 아래 4가지 기법을 실제 적용해 효과를 확인함:

1. **히든 로그 대조법** — 완료 보고에 안 나오는 백그라운드 로그(`Global_Define\global_watcher.log`)를 직접 확인. → **실제 발견**: 지금도 `git add .`가 exit 128로 반복 실패 중(아무도 보고 안 함). 만복에게 별도 알림 예정.
2. **코드 실체 대조법** — "구현했다"는 말을 믿지 않고 TODO/주석 처리된 줄을 직접 대조. → T022에서 실제 미연동 부분 발견.
3. **잔여 데이터 역추적법** — 아무도 언급 안 한 데이터 파일 자체를 뒤져 실사용 흔적을 찾는다. → T020에서 보고와 모순되는 실제 요청 3건 발견.
4. **적대적/실패 시나리오 요구** — 성공 케이스 로그만 받지 말고 "반려 누르면", "토큰 만료되면" 같은 실패 시나리오 로그도 별도 요구. (다음 검증 때 적용 예정)

## 실사용 효과 검증 (역할/지향점 대비, 코드검증과 별개 레이어) — 2026-07-12 코니

코드/로그 검증(위 표)은 "구현 검증"일 뿐이고, 아래는 각 항목이 만들어진 목적을 실제로 달성하고 있는지를 보는 "효과 검증"임.

| ID | 지향점(만든 이유) | 효과 검증 질문 | 현재 판단 |
|----|---------------|--------------|--------|
| T019 | API 비용 없이 Genspark급 리서치 속도 | 실제 리서치 업무(특허 선행조사, AI Study 자료조사 등)에 한 번이라도 대체 투입되었는가? 기존 방식(WebSearch 수동) 대비 속도/품질이 실제로 나았는가? | ❌ 실전 투입 사례 없음 — 지향점 대비 효과 거의 0 |
| T020 | 자동화가 실행되기 전 바로보기님이 실제 거부권 행사 | (1) 이 게이트를 실제로 물고 있는 다른 파이프라인(T063, T022)이 하나라도 있는가? (2) 바로보기님이 실제로 텔레그램 승인/반려 버튼 메시지를 받아본 적 있는가? | ❌ (1) 연동된 파이프라인 없음(T022도 미연동) → 아무것도 게이트하고 있지 않음. (2) **바로보기님께 직접 확인 필요**: 실제 승인 요청 메시지를 받아보신 적 있나요? |
| T022 | 배포 전 실제 위험(취약점) 차단해 리스크 감소 | 스캔 대상(주로 GitHub Pages 정적 사이트, 백엔드 없음)에 ZAP이 잡을 만한 실제 취약점이 애초에 있는가? 잡아낸다면 의미있는 발견인가, 아니면 요식행위인가? | ⚠️ 미판단 — 대상 에서 프로젝트 구성(정적 사이트 위주)을 감안하면 "잡을 만한 것이 적을" 가능성 있음 — 첫 스캔 결과 나오면 "진짜 위험 발견" vs "노이즈" 비율로 판단 |

**공통 결론**: 세 항목 모두 "만들어졌다"와 "목적을 실제로 달성하고 있다" 사이에 간극이 있음. 특히 T020은 기술적으로 완벽해도 연동된 파이프라인이 없으면 "아무것도 통제하고 있지 않은" 상태로 실효성 0이 될 수 있음 — 착수된 다른 파이프라인이 생기는 시점까지 지켜볼 것.

### T019 실사용 검증 계획 (2026-07-12 확정)

**배경**: T019(parallel_search.py)는 원래 코니 전용으로 기획되었다가, 바로보기님이 3AI 동등 능력 확보 차원에서 범용 도구로 확장함.

**제약**: 코니는 D:\AI에 파일 읽기/쓰기는 가능하나, 해당 머신에서 파이썬 스크립트를 직접 실행할 셔럴 접근 권한이 없음(별개 샌드박스 환경). 실행 권한은 만복/안티에게 있음.

**실행 계획**: 실제 리서치가 필요한 작업(특허 선행조사, AI Study 자료조사 등)이 생길 때마다:
1. 코니가 만복 또는 안티에게 "이 쿼리로 parallel_search.py 돌려줘"라고 요청
2. 동시에 코니가 직접 WebSearch로 같은 주제를 검색
3. 두 결과의 속도·품질·비용을 실제로 비교해 이 파일에 사례로 누적

**저조 이용 자체가 판정근거**: 요청을 했는데 만복/안티가 계속 미루거나 미반응이면, 그 자체가 "이 도구가 실제 워크플로에 녹아들지 못했다"는 효과 검증 결과로 간주 — 이 경우 ⚫방치로 내려 바로보기님께 보고.

**사용 이력**: (아직 없음 — 첫 요청/응답 발생 시 날짜, 요청자, 속도/품질 비교 여기 기록)

## 검증 원칙

1. tasks.json 상태만 보고 "완료"로 인정하지 않는다 — 실제 산출물(파일/로그) 존재 여부를 코니가 직접 Read로 확인한다.
2. "완료 보고 = 증거 첨부" 원칙([[project_3ai_governance]])에 따라, 안티/만복이 첨부한 증거가 있으면 그 증거 자체를 재확인(파일 존재·내용 일치)한다.
3. 바로보기님 액션이 필요한 항목(예: 봇 토큰 발급)은 표에 굵게 별도 표시.
4. **안티 작성 항목은 "만복 검수 통과"라는 말만으로 🟢을 주지 않는다** — 코니나 만복이 독립적으로 파일/로그를 재확인한 뒤에만 🟢으로 승격한다 (2026-07-11 안티 Hall 패턴 때문에 보수적으로 운영).

## 검증 이력 (세션 종료 시 게시물 링크 기록)

_(아직 없음 — 첫 세션 종료 시 logs.html 게시물 등록 후 여기 링크/data-id 추가)_

*최초 작성: 2026-07-12 (코니)*
