---
sender: 만복
recipient: 안티
title: [GPS 지시서] 오늘뭐하지 앱 Phase 0 마무리 + Phase 1/2 착수
date: 2026-08-20
status: triggered
---

안티야, 지시서 하나 남긴다 (만복 세션 컨텍스트 얼마 안 남아서 이어받아줘) - GPS 형식으로 정리한다.

## Goal
"오늘뭐하지" 앱(D:\AI\65_android_apps\today_what_to_do\) Phase 0 마무리 + Phase 1(Backend)/Phase 2(추천엔진) 착수. 목표: 실제 GPS 좌표 기준으로 관광지/반려동물동반 장소 데이터를 모아서, AI 없이 규칙기반(Hard Filter)으로 "오늘 갈 만한 곳"을 걸러내는 게 실제로 동작하는지 증명.

## Proof (완료 기준 - 전부 실행 결과 캡처 첨부)
1. `locationBasedList2`로 서울 주요 좌표 3곳 이상(예: 시청/강남역/홍대) 실호출 → 응답 JSON 필드 구조를 `docs/api/kto_tourism.md`에 실제 예시로 추가
2. `detailIntro2`/`detailCommon2`로 운영시간·휴무일 실제 필드명 확인 → 문서화
3. 반려동물 API(`KorPetTourService2`)도 동일하게 `locationBasedList2` + `detailPetTour2` 실호출 → `docs/api/kto_pet_tourism.md`에 반영
4. 위 결과로 실데이터 100건 이상 수집해서 로컬 저장 (SQLite 또는 JSON, `backend/data/` 폴더 신설해서)
5. 기획서 9.1절 Hard Filter를 순수 파이썬 함수로 구현 (`backend/recommend/hard_filter.py`) - AI 호출 없이:
   - is_open 체크
   - price > budget 체크
   - distance > max_distance 체크
   - pet_allowed 체크
   - 날씨(기상청 API POP값) > 60%면 실외 감점
6. 테스트: "7세 아이/서울/오늘/3시간/3만원/대중교통/비" 시나리오로 Hard Filter 돌려서 실제로 후보가 걸러지는지 터미널 출력 캡처

## Steps
1. `.env`는 이미 `65_android_apps/today_what_to_do/.env`에 3개 키 다 있음 (git 커밋 금지, 이미 gitignore 처리됨)
2. `phase0/test_three_apis.py` 참고해서 API 클라이언트 확장
3. 위 Proof 1~6 순서대로
4. 완료되면 `docs/ROADMAP.md`의 Phase 0/1/2 체크박스 갱신
5. tasks.json T065ROOT_today_what_to_do에 진행상황 append
6. 완료보고는 코니(검증)한테 - 근데 지금은 코니가 "Phase1 끝나면 합류하겠다"고 한 상태니, 코드 대조 검증 요청할지는 네 판단. 급하면 그냥 나(만복)한테 결과 보고해도 된다.

git 커밋할 때는 반드시 협조락 써라:
python D:\AI\Global_Define\git_sync_lock.py acquire
(git 작업)
python D:\AI\Global_Define\git_sync_lock.py release

템플릿화는 아직 하지 마라 - Phase 1 끝나고 바로보기님이랑 다시 판단하기로 합의됨(ROADMAP.md 참조).