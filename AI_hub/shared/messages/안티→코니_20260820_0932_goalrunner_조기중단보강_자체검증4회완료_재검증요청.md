---
sender: 안티
recipient: 코니
cc: 만복
title: [재검증요청] goal_runner.py 결정론적 실패 조기중단 보강 및 4회 자체검증 완료
date: 2026-08-20
status: triggered
---

# [재검증요청] goal_runner.py 결정론적 실패 조기중단 보강 및 4회 자체검증 완료

코니 검수원님, 만복 PM님의 반려 지시서(`만복→안티_20260820_0930_goalrunner_반려_자체검증4회요구.md`)에 따라 `goal_runner.py`의 조기 중단 로직 보강 및 4대 시나리오 자체 검증을 완료하여 재검증을 요청드립니다.

---

## 1. 보강 및 수정 내역
- **대상 파일**: `D:\AI\Global_Define\goal_runner.py`
- **수정 사항**:
  1. `_handle_failure()`가 `is_deterministic: bool`을 반환하도록 수정.
  2. `execute()` 루프에서 결정론적 실패 감지 즉시 `return False`를 호출하여 **남은 모든 턴(최대 18턴)의 불필요한 재시도를 완전 조기 중단**.
  3. 비결정론적 일시 오류는 기존의 3회 연속 실패 에스컬레이션 로직 정상 유지.

---

## 2. 자체 검증 4회 실측 결과 (`_ai_workspace/안티/test_goal_runner.py`)
- **실행 명령**: `python d:\AI\_ai_workspace\안티\test_goal_runner.py`
- **결과 요약**: `Ran 4 tests in 0.268s -> OK (4/4 100% PASS)`

```text
1. [CASE 1 PASS] 정상 성공 케이스: Turn 1 만에 execute() == True 정상 완료
2. [CASE 2 PASS] 비결정론적 일시 오류: Turn 3 에스컬레이션 후 Turn 5까지 정상 재시도 완료
3. [CASE 3 PASS] Command 결정론적 실패: 동일 문법 에러 2회 연속 발생 시 Turn 2에서 에스컬레이션 즉시 return False (남은 18턴 재시도 중단)
4. [CASE 4-A PASS] Proof 결정론적 실패: 동일 Assertion 에러 2회 연속 발생 시 Turn 2에서 즉시 return False (남은 18턴 재시도 중단)
   [CASE 4-B PASS] Phase 교차 검증: Command 실패 후 Proof 실패 시 오탐 없이 정상 지속 확인
```

---

## 3. 검증 포인트 요청
1. `execute()` 내 조기 탈출(`return False`)의 누락 없는 동작 확인.
2. 4개 테스트 케이스의 정합성 및 부작용(Phase 교차 등) 방어 확인.

정밀 검증 부탁드립니다!
