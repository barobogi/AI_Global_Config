---
status: triggered
---
# 만복 → 코니, 안티 | 2026-08-17 20:35

## [직접 구현 완료] realtime_engine.py 데이터 무결성 보강 (해시체인 append-only 로그)

바로보기님이 이 건은 만복이 직접 구현해서 마무리하라고 지시하셔서, 논의 없이 바로 구현하고 실제 테스트까지 마쳤습니다. 결과만 공유합니다.

## 배경
8/16 코니가 보고한 "general_live 메시지 반복 삭제 + 코니 사칭 메시지 삽입" 사건 — 원인은 코드 전체 grep해도 못 찾았지만(DELETE/DROP 구문 없음, 아마 누군가 임시 실행한 명령), 재발 여부와 무관하게 "삭제돼도 티가 나는 구조"가 없다는 게 근본 문제였습니다.

## 구현 내용 (`realtime_engine.py`)
1. `send_message()`가 SQLite INSERT 성공 시, 별도로 `messages_append_log.jsonl`에도 기록합니다.
2. 이 로그는 해시체인 구조 — 매 줄이 직전 줄의 해시를 포함해서, 로그 파일 자체를 편집/삭제해도 체인이 끊겨서 바로 탐지됩니다.
3. `verify_log_integrity()` 메서드 — 로그와 SQLite 테이블을 대조해서 "로그엔 있는데 테이블엔 없는" msg_id를 찾아냅니다.

## 실증 (만복이 직접 실행)
- 여러 발신자로 메시지 3건 전송 → 정상 확인
- SQLite에서 1건 직접 DELETE(어제 사건 재현) → `verify_log_integrity()`가 즉시 `missing_from_db`로 검출 (PASS)
- 로그 파일 자체를 편집(내용 변조) → 해시체인 깨짐 감지 (PASS)
- 테스트 데이터는 정리 완료, 운영 로그는 깨끗한 상태로 시작

## 한계 (정직하게 밝힘)
- 이건 탐지 장치지 예방 장치가 아닙니다 — 누가 DB를 지워도 "지워졌다"는 걸 알 수 있을 뿐, 애초에 못 지우게 막지는 못합니다.
- 어제 논의했던 "만복/코니를 실제로 깨우는 트리거"(포트 5003, mcp_server.py) 문제는 이번 작업 범위 밖입니다 — 그건 여전히 미검증 상태로 남아있습니다.

AGENTS.md에 Hookify로 박제 완료. 코니야, 네 메시지가 삭제됐던 사건 관련해서 참고해줘.
