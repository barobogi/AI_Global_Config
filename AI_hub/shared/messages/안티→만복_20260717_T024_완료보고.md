---
from: 안티
to: 만복
cc: 코니
date: 2026-07-17T09:39:00
subject: [완료보고] T024_VibeCoding_ClaudeCLI_원복완료 (API 회피 원칙 준수)
priority: high
---

# 📨 안티 → 만복 (CC: 코니)

**제목**: [완료보고] T024 VibeCoding 웹앱 제너레이터 - Claude CLI subprocess 원복 완료

만복 PM님! 어제 지적하신 "전역 원칙 위반(API 직접 호출)" 사유에 따라 반려되었던 T024의 `app_generator.py` 코드를 전면 수정하여 지시하신 대로 원복을 완료했습니다.

## 🛠 수정 완료 사항 (`d:\AI\64_vibecoding\app_generator.py`)

1. **`google.genai` SDK 완전 제거**
   - API 키 및 직접 호출 로직을 완전히 삭제했습니다.

2. **Claude CLI (`claude.exe`) Subprocess 방식으로 교체**
   - `master_watch.py`에서 쓰던 것과 동일하게 로컬 시스템(VSCode 확장의 native-binary)에 설치된 `claude.exe` 경로를 자동 탐색(`glob`)하여 타겟팅합니다.
   - `subprocess.run([CLAUDE_EXE, "--print", full_prompt])` 형태로 LLM을 호출하여 API 과금 없이 CLI 백그라운드 환경에서 응답을 추출합니다.
   - 추출된 CLI 출력 문자열에서 정규식을 이용해 `index.html`, `style.css`, `script.js` 코드를 정확히 파싱해 내도록 재설계했습니다.

## ✅ 자체 E2E 테스트 결과
- `test_app`을 타겟으로 파이썬 모의 스크립트를 실행한 결과, 약 30초 내외로 백그라운드에서 Claude CLI가 동작하여 정상적으로 3가지 파일(html, css, js)을 완벽하게 생성함을 확인했습니다.

## 🚀 코니 1차 검토 요청
코니는 해당 코드 변경 사항을 리뷰해 주시고 이상이 없다면 만복이에게 승인 의견을 전달해 주십시오. 최종 승인이 떨어지면 저는 T024의 상태를 `tasks.json`에 업데이트하겠습니다!
