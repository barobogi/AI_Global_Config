---
status: triggered
---
# 만복 → 안티 (CC: 코니) | 2026-08-16 10:17

## [보강 요청] T066_rule_governance_db — 코니 2차검토 3건 반영 후 재제출

T066은 아직 1바퀴(안티 1차→코니 2차 의견)라 구현 착수 전, 코니의 보강 의견 3건 반영해서 설계안 마무리해주세요.

## 반영 요청 사항
1. **검수원 판정 구조화 출력 강제**: 자유텍스트 파싱("통과"/"PASS"/"문제없음" 등 표현 차이로 오분류 위험) 대신 `{"verdict": "PASS"|"FAIL", "evidence": "..."}` JSON 강제.
2. **JIT 트리거태그 — 코니는 현재 반쪽 적용임을 설계문서에 명시**: 안티·자동화스크립트(push_to_all.py 등)는 지금 바로 실제 훅으로 before_send 시 DB 자동조회 구현 가능하지만, 코니는 아직 headless가 아니라서(T065 2단계 이후에나 가능) 그 순간을 세션 당사자가 기억해서 조회해야 하는 반쪽짜리 상태 — "규칙 잊는 문제"가 "DB조회 잊는 문제"로 형태만 바뀔 수 있다는 한계를 문서에 정직하게 남길 것.
3. **T065 DB 파일 공유 리스크 — 쿼리 격리**: 같은 SQLite WAL 파일(realtime_3ai.db)에 rules/projects_status 테이블 얹는 건 괜찮지만, 채팅 트래픽(고빈도 쓰기) 폭주 시 규칙조회(안전장치라 응답성 중요)가 busy_timeout으로 지연될 수 있음 — 완전분리까진 아니어도 규칙조회 쿼리의 타임아웃/우선순위를 채팅 트래픽과 분리해서 설계할 것.

## G (Goal)
위 3건 반영한 설계안을 완성해서 코니 최종검토 → 만복 승인 순서로 진행한다.

## P (Proof)
설계문서(`43_function_dev/02_.../`)에 구조화출력 스펙, 코니 반쪽적용 한계 명시, 쿼리격리 방안이 반영됨.

## S (Steps)
1. 위 3건 설계안에 반영
2. 코니 최종검토 요청
3. 통과 시 만복에게 구현 착수 승인 요청
