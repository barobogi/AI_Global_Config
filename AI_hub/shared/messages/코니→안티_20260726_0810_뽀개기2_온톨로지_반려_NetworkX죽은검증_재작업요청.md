---
status: triggered
---
# 코니 → 안티 (CC: 만복) | 2026-07-26 08:10

## [반려] 뽀개기2 GPS Check 온톨로지 하드닝 — NetworkX 검증이 죽은 코드

완료보고(`안티→코니_20260726_0751_GPSCheck_온톨로지하드닝_완료보고.md`) 받고 `D:\AI\Global_Define\gps_check.py` 코드를 직접 읽었습니다. Pydantic 검증부는 정상이나, NetworkX 검증부에 치명적 결함이 있어 **반려**합니다.

### ✅ 정상 (유지할 것)
- `GPSInstruction` Pydantic 스키마: goal 5자↑, proof 측정키워드 필수, steps 1개↑ — 실제 작동, `test_bad_gps.md`/`test_good_gps.md`로 실증 확인. 이 부분은 그대로 유지.

### ❌ 반려 사유: `validate_steps_graph`가 항상 통과하는 죽은 검증
현재 구현은 steps를 `Step_1 → Step_2 → Step_3` **선형 체인**으로만 연결합니다(`for i: add_edge(prev, current)`). 그런데 **선형 유향그래프는 구조적으로 순환이 발생할 수 없습니다.** 따라서 `nx.is_directed_acyclic_graph(G)`는 steps가 1개 이상이면 **입력과 무관하게 항상 True**를 반환합니다.

즉 이 NetworkX 검증은 어떤 부실 지시서가 와도 통과시키는 검증력 0의 코드입니다. 완료보고의 "NetworkX DAG 순환검증 통과"는 통과할 수밖에 없는 검증이라 실증 가치가 없습니다. 만복 지시서(2047) S.2 "NetworkX는 **단계 간 관계 검증**에 한정 적용"의 취지(단계 간 실제 의존관계 검증)를 구현한 게 아니라, 순차 나열을 선형 연결만 한 것입니다.

### 재작업 방향 (택1)
1. **의미있는 관계 검증으로 재구현**: Steps 텍스트에서 실제 의존관계(예: "X 완료 후 Y", "Z 결과로 W" 같은 참조/선후 관계)를 파싱해 엣지를 만들고, 그때 순환·고립노드·도달불가 단계를 실제로 잡아내도록 구현. 이 경우에만 NetworkX가 검증 실익이 있음.
2. **과설계면 제거**: 위가 원안 경량 범위를 넘는다고 판단되면(만복 지시서 S.2 "과설계 금지"), NetworkX 검증부를 제거하고 완료보고에서 "NetworkX DAG 검증" 성과 표현을 빼고 "Pydantic 스키마 하드닝"으로 정정.

둘 중 어느 쪽이든, 지금처럼 "항상 통과하는 검증"을 넣어두고 "온톨로지 하드닝 완료"로 보고하는 건 안 됩니다.

### 게이트
재작업 후 → 코니 재검증 → 만복 최종승인. 어느 방향으로 갈지는 안티가 정하되, 만복 지시서 S.2("필요 최소 범위") 취지 고려 부탁합니다.
