---
status: triggered
---

# 📋 [구현 지시] 뽀개기 자동화 파이프라인 v2.0

**발신:** 만복 (바로보기님 직접 지시)
**수신:** 안티
**CC:** 코니
**작성일:** 2026-07-19
**우선순위:** 🔴 최우선

---

## G (Goal)
매일 18:00에 자동으로 실행되어 등록 채널에서 6개 영상을 선별하고, 만복이 3건 자막 추출+분석을 완료한 뒤 안티에게 3건 전달 — 바로보기님이 20:30 귀가 시 리뷰 가능한 상태로 준비되어 있는 파이프라인

## P (Proof)
- master_watch.py에 18:00 크론 등록 확인
- 실제 실행 시 channels.json → RSS 폴링 → 6개 선택 → 만복 자막 추출 → 안티 inbox 전달까지 전체 흐름 테스트 통과
- 텔레그램으로 완료 알림 수신 확인

## S (Steps)

---

### 1단계: 코니 Critical 수정 (기존 코드 보강)

**1-1. API 키 분리**
- `D:\AI\25_auto_pobbagi\config.json` 생성
- Gemini API 키를 config.json에 저장
- `auto_stt_gemini.py`, `auto_pobbagi_orchestrator.py`에서 config.json 읽도록 수정
- `.gitignore`에 config.json 추가

**1-2. XML 파싱 예외처리**
- `youtube_rss_watcher.py` Line 43 `ET.fromstring(xml_data)`를 try-except로 감싸기
- 파싱 실패 시 빈 리스트 반환 (graceful degradation)
- 각 entry 파싱도 개별 try-except 처리

**1-3. 임시 파일 정리**
- `auto_stt_gemini.py` 전체 함수를 try-finally로 감싸기
- finally 블록에 `os.remove(audio_path)` 추가 (실패해도 계속 진행)

**1-4. 코니 Warning 수정**
- State 전이 로직 명확화: `approve_queue()` 함수 추가 (waiting_review → approved/rejected)
- 타임스탬프 로그 파일 추가 (`D:\AI\25_auto_pobbagi\logs\YYYYMMDD.log`)
- Gemini API exponential backoff 재시도 로직 추가 (Rate Limit 대비)

---

### 2단계: channels.json 채널 ID 실제 등록

`D:\AI\25_auto_pobbagi\channels.json` 파일이 이미 생성되어 있음.
아래 6개 영상에서 채널 ID를 추출해서 channels.json의 placeholder(HOODU_AI 등)를 실제 채널 ID로 교체:

| 영상 ID | 채널 |
|---------|------|
| J4bBeU3frnE | 호두의 AI 분석실 |
| gUGVPxF3c_s | 동기부여학파 |
| c-LGpvBuwdk | Matt Pocock |
| jdbOVepEtUE | Claude Code for Non-Coders |
| uED-1uD563Y | Hermes Agent 채널 |

YouTube RSS URL: `https://www.youtube.com/feeds/videos.xml?channel_id={CHANNEL_ID}`
영상 페이지 HTML에서 채널 ID 추출 또는 YouTube API 활용.

---

### 3단계: 핵심 스크립트 신규 작성

**`D:\AI\25_auto_pobbagi\daily_pobbagi_runner.py`**

```python
# 매일 18:00 자동 실행되는 뽀개기 파이프라인 메인 스크립트

흐름:
1. channels.json 로드
2. 각 채널 RSS 폴링 → 신규 영상 수집
3. 신규 없는 채널 → 과거 영상 풀에서 보충 (조회수/댓글 기준 명작 선택)
4. 제목 1차 필터링 (AI/자동화/개발/창업/에이전트 키워드 + 이미 뽀개기한 영상 제외)
5. 후보 풀에서 parallel_search.py로 Deep 서치 → 뿌리체계 연결도 점수화
6. 상위 6개 선택 (만복 3 + 안티 3 자동 배분)
7. 만복 3건: auto_stt_gemini.py 순차 실행 (자막 추출 + transcripts/ 저장)
8. 만복 분석: 각 영상 핵심 요약 + 뿌리 연결 초안 작성 → review_ready/ 저장
9. 안티 3건: AI_hub/shared/messages/만복→안티_YYYYMMDD_뽀개기3건_지시.md 자동 생성
10. push_to_all.py 실행 (안티 격발)
11. 텔레그램 완료 알림: "🎯 오늘 뽀개기 준비 완료\n[제목1]\n[제목2]\n[제목3]\n귀가 후 리뷰 가능"
```

**`D:\AI\25_auto_pobbagi\channel_manager.py`**
```python
# 텔레그램으로 채널 등록/삭제 관리
# 바로보기님이 텔레그램에 유튜브 링크 전송 → 채널 자동 추출 → channels.json 업데이트
```

**`D:\AI\25_auto_pobbagi\pobbagi_history.db`** (SQLite)
```sql
-- 뽀개기 이력 DB
CREATE TABLE pobbagi_history (
    video_id TEXT PRIMARY KEY,
    title TEXT,
    channel_id TEXT,
    channel_name TEXT,
    pobbagi_date TEXT,
    assignee TEXT,  -- manbok / anti
    status TEXT,    -- completed / skipped
    root_id INTEGER,
    score REAL
);
```

---

### 4단계: master_watch.py 18:00 스케줄 등록

`D:\AI\Global_Define\master_watch.py`에 아래 추가:
```python
# 매일 18:00 뽀개기 자동 실행
if current_time == "18:00" and not already_run_today("pobbagi"):
    subprocess.Popen(["C:/hb/python.exe", "D:/AI/25_auto_pobbagi/daily_pobbagi_runner.py"])
    mark_run_today("pobbagi")
```

---

### 5단계: 채널 가중치 학습 (기본 구조)

뽀개기 완료 후 바로보기님이 "좋음/패스" 피드백 → channels.json의 weight 자동 업데이트
- 좋음: weight += 0.1 (최대 2.0)
- 패스: weight -= 0.05 (최소 0.5)
→ 다음 날 6개 선택 시 weight 기반 채널 우선순위 반영

---

## 작업 순서

1. 코니 Critical 수정 (1단계) → 만복 확인
2. channels.json 채널 ID 교체 (2단계)
3. daily_pobbagi_runner.py 작성 (3단계 핵심)
4. master_watch.py 스케줄 등록 (4단계)
5. 전체 E2E 테스트 (오늘 저녁 18:00 실제 실행 확인)
6. 만복에게 완료 보고 → 바로보기님 최종 확인

---

## 주의사항

- API 비용 0원 원칙: Gemini Flash STT는 무료 티어 범위 내 사용 확인
- 텔레그램 알림은 기존 telegram_messages.py 활용
- push_to_all.py mcp_server 꺼진 경우 → inbox.md 파일 기반으로 폴백 처리
- 만복이 직접 STT 실행하므로 안티는 안티 담당 3건만 처리 (중복 없도록)

완료 후 만복에게 보고. 코니가 2차 검증 진행.
