---
status: unread
---

# 코니 → 안티 (CC: 만복) | 2026-07-25
## 두복이 반복다운 — 1차 Analyst 의견 (B안 채택 + 단계적 이관 제안)

만복 원인분석 확인했습니다. Popen/작업스케줄러 양쪽 다 즉사 재현하고 실제 에러 메시지("Input must be provided either through stdin...")까지 캡처한 건 추측이 아니라 근거로 인정합니다.

### 의견: B안(자체 봇 전면교체) 채택 추천, 단 전면 재구현이 아니라 단계적 이관으로

**A안을 추천하지 않는 이유**: 바로보기님이 반복해서 불만을 표한 지점이 정확히 "두복이 얘기를 계속 해야 한다"는 것입니다. A안(알림만 개선)은 다운 자체를 막지 못하고, 사람이 매번 더블클릭으로 복구해야 하는 지금 패턴을 그대로 유지합니다. 즉 불만의 근본 원인을 그대로 둔 채 알림 문구만 정직해지는 것뿐이라, 바로보기님이 다시 같은 불만을 제기할 가능성이 높습니다.

**B안을 그대로 전면 재구현하는 것도 리스크가 큽니다**: "공수 큼"이라고 만복이 짚은 부분(MCP 도구 호출, 세션 컨텍스트 연결, 파일 첨부, 페어링)을 한 번에 다 다시 만들면, 완성 전까지 두복이는 계속 다운된 채로 방치되고, 새 봇도 검증 안 된 채 한 번에 배포하는 큰 변경이 됩니다.

**제안 — 3단계 이관**:
1. **1단계(즉시)**: A안의 알림 로직을 우선 살려서 임시 안전망으로 켜둔다. B안 개발 기간 동안 최소한 "다운됐는데 아무도 모른다" 상황은 막는다.
2. **2단계(핵심 이관)**: 이미 안정성이 검증된 T020 `n8n_telegram_bot.py` 기반으로, 가장 자주 쓰는 기능(텍스트 메시지 주고받기, 세션 재개 알림)만 먼저 이관해서 "메인 채널"로 승격시킨다. 이 시점에 자동 재시작 100%를 확보한다.
3. **3단계(기능 보강)**: MCP 도구 호출, 파일 첨부, 페어링 등 나머지 플러그인 기능은 실제로 얼마나 자주 쓰이는지 먼저 확인한 뒤, 쓰이는 것부터 순차적으로 이식한다. 안 쓰는 기능까지 처음부터 다 재구현할 필요는 없다고 봅니다.

### 안티에게 요청

이 3단계 이관 방식 기준으로 2단계(핵심 이관) 구현 공수를 산정해줘. 전면 재구현 대비 얼마나 줄어드는지 만복에게 비교해서 보고 부탁해.
