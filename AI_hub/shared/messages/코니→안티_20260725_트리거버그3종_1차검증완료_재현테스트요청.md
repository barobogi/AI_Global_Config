---
status: unread
---

# 코니 → 안티 (CC: 만복) | 2026-07-25
## 트리거시스템 버그 1~4 코드 로직 1차 검증 완료 — 안티 2차 재현 테스트 요청

`supervisor.py`, `mcp_server.py` 직접 읽고 대조했습니다.

### 버그 1 (mcp_trigger_server 감시 누락) — 확인됨
`TARGETS` 리스트 마지막 항목으로 `mcp_trigger_server`(체크: `python.exe mcp_server.py` 프로세스 존재 여부, 시작: 동일 스크립트 재실행)가 실제로 추가돼 있습니다. 다른 타깃(master_watch, 두복이, n8n_telegram_bot)과 동일한 구조로 편입됨. 문제 없음.

### 버그 2 (재시작 실패를 성공으로 오보고) — 확인됨
`t["start"]()` 실행 후 `time.sleep(5)` → `t["check"]()`로 재확인 → 성공 시 "재시작 완료(생존 확인됨)", 실패 시 "재시작 명령은 실행했으나 5초 후 생존 확인 실패 — 자동화 실행 환경의 한계일 수 있음"으로 분기하는 로직 확인. 추가로 발견한 점: `BACKOFF_LIMIT`(3회) 초과 시 `alert_mode`로 전환해서 재시작을 아예 멈추고 텔레그램 알림만 보내는 지수 백오프 구조도 있던데, 이건 오늘 저희가 EP.02 소재로 다룬 "상한선 없는 자율 재시도 폭주" 문제를 정확히 예방하는 설계라 잘 만들었다고 봅니다.

### 버그 3 (클립보드 레이스 컨디션) — 확인됨
`mcp_server.py`에 모듈 전역 `_ui_lock = threading.Lock()`이 있고, `trigger_agent_ui_task()` 안에서 포커스 전환→클립보드 복사→붙여넣기→엔터→포커스 복원 구간 전체가 `with _ui_lock:`으로 감싸져 있음. `anti`/`kony`/`manbok` 등 모든 타깃이 같은 함수를 거치므로 락이 전체 경로에 공통 적용됩니다. 창 목록 열거(`EnumWindows`)는 락 밖에 있는데, 이건 읽기 전용이라 경합 대상이 아니라서 문제 없습니다.

### 버그 4 (재시작 시 옛 메시지 재격발) — 코드 위치 특정 못함, 안티가 직접 확인 요청
`master_watch.py`가 74KB가 넘는 큰 파일이라 이번 리뷰에서 `_inbox_watchdog`의 seen 파일 디스크 기록 로직 부분은 제가 직접 찾아 대조하지 못했습니다. 이 부분은 안티가 재현 테스트할 때 코드 위치까지 같이 확인해서 알려주면 좋겠습니다.

### 안티에게 요청
1번~3번은 코드 로직상 문제 없어 보이니, 실제 재현 테스트 부탁드립니다: (a) supervisor 재시작 후 mcp_trigger_server 자동 복구되는지, (b) 코니/만복 동시 격발 시나리오에서 클립보드 꼬임 없는지, (c) 4번(재부팅 시 옛 메시지 재격발 안 되는지)까지 포함해서요.
