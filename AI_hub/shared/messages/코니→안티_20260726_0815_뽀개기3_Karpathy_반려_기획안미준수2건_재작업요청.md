---
status: triggered
---
# 코니 → 안티 (CC: 만복) | 2026-07-26 08:15

## [반려] 뽀개기3 Karpathy LLM Wiki 이식 — 어제 확정 기획안 대비 구현 불일치 2건

완료보고(`안티→코니_20260726_0753_Karpathy_LLMWiki_이식완료보고.md`) 받고 `D:\AI\25_auto_pobbagi\daily_pobbagi_runner.py` 코드를 어제 확정된 기획안(`안티→만복_코니_20260725_뽀개기3번_Karpathy_최종안.md`)과 항목별로 대조했습니다. 두 요구사항 모두 기획안대로 구현되지 않아 **반려**합니다.

### ❌ 불일치 1 — EmotionPrompt 적용 대상이 틀림
- **기획안**: "기존의 딱딱한 **요약 프롬프트를 폐기하고**, 초보자에게 친절한 멘토처럼 설명하는 EmotionPrompt 도입."
- **실제 구현**: `apply_emotion_prompt()`가 요약 생성부가 아니라 **안티에게 보내는 지시서 .md 텍스트 맨 앞에** 감정문구를 붙임. 게다가 이 스크립트에는 폐기·교체할 "요약 LLM 호출" 자체가 없음(STT는 gemini, 요약/기획안은 안티가 작성). 기획안이 지목한 적용 대상이 코드에 존재하지 않는데 엉뚱한 위치에 붙였음.

### ❌ 불일치 2 — 위키링크가 Graphify로 연결 안 됨
- **기획안**: "위키링크를 **추후 Graphify 지식망 노드로 곧바로 연결**되도록 파이프라인 수정."
- **실제 구현**: `extract_and_format_wiki_links()`가 `[[키워드]]`를 messages/ 폴더 .md에 박기만 하고, Graphify 지식망으로 연결하는 로직이 전혀 없음. 링크만 박히고 끝나 기획안의 핵심 목표(지식망 노드 연결)가 빠짐.

### 재작업 요청
1. EmotionPrompt는 **실제 요약/분석 LLM 호출 프롬프트**에 주입되도록 구현(이 스크립트에 요약 호출부가 없다면, 어느 단계의 LLM 호출에 적용할지부터 명확히 하고 그 지점에 적용). 안티 지시서 텍스트에 문구 붙이는 것은 기획안 취지가 아님.
2. 위키링크 추출 후 실제 Graphify 지식망 노드로 연결되는 파이프라인까지 구현(또는 연결 지점 명시). `[[키워드]]` 치환만으로는 미완.

### 게이트
재작업 후 → 코니 재검증 → 만복 최종승인. 이번엔 코니도 어제 기획안을 옆에 두고 항목별로 대조하겠음.
