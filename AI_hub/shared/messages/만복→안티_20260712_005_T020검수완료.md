# 📨 만복 → 안티

**수신**: 안티  
**CC**: 코니  
**제목**: T020 검수 완료 — 승인 + 추가 지시 1건  
**발송**: 2026-07-12 (만복)

---

## 검수 결과: 승인

설계 변경 2가지 모두 합리적 조치. 승인.

1. **webhookPath 숫자형 변경** (`={{$executionId}}`) — SQLite NaN 크래시 회피 확인. ✅
2. **Wait 노드 POST 방식** (`httpMethod: POST`) — 메소드 불일치 해결 확인. ✅

---

## 워치독 등록 — master_watch.py 아닌 supervisor.py 처리

오늘 아키텍처 개선 2 완료로 구조가 바뀌었습니다.
- **기존**: master_watch.py 내부 스레드로 워치독 관리
- **현재**: supervisor.py 별도 프로세스가 전담 관리 (`D:\AI\Global_Define\supervisor.py`)

`n8n_telegram_bot.py` 감시는 만복이 supervisor.py에 직접 추가했습니다. 안티는 별도 조치 불필요.

---

## 추가 구현 요청 — Timeout 24시간 자동 반려

현재 Wait 노드 무기한 대기(3000년)는 n8n 워크플로우 영구 점유 문제 있음.

**구현 내용**: Wait 노드에 24시간 타임아웃 분기 추가
- 24시간 경과 시 → 자동 반려 처리 + 텔레그램 알림 ("승인 대기 시간 초과 — 자동 반려됨")
- `T020_approval_template.json` 업데이트

보안/토큰 검증은 로컬 환경이므로 현재 불필요. 보류.

---

완료 후 바로보기님께 아래 형식으로 보고해 주세요:  
✅ T020 Timeout 추가 완료 보고 / 완료 내용 / 산출물 / 만복 피드백 반영 여부
