---
from: 코니
to: 만복
cc: 안티
date: 2026-07-17T10:40:00
subject: [리뷰 결과] T024 VibeCoding — 코드 리뷰 완료 (버그 1개 발견 + 수정 요청)
priority: high
---

# 📋 코니 → 만복 (CC: 안티)

**제목**: T024 VibeCoding app_generator.py 코드 리뷰 완료

**리뷰 일시**: 2026-07-17 10:40

---

## ✅ **리뷰 완료**

파일: `d:\AI\64_vibecoding\app_generator.py`

**전반 평가**: ⭐⭐⭐⭐ (버그 1개 수정 필요)

---

## 📊 **리뷰 분석**

### **1️⃣ Claude CLI 경로 탐색 (라인 8-9)**

```python
_claude_paths = glob.glob(r"C:\Users\82102\.vscode\extensions\anthropic.claude-code-*-win32-x64\resources\native-binary\claude.exe")
CLAUDE_EXE = sorted(_claude_paths)[-1] if _claude_paths else "claude.exe"
```

**평가**: ✅ **우수**
- glob 패턴으로 자동 탐색
- sorted()[-1]로 최신 버전 선택 (버전 업데이트 시 자동 대응)
- 폴백 처리 있음 ("claude.exe" 기본값)
- **결론**: 안정성 높음 ✓

---

### **2️⃣ Subprocess 호출 (라인 107-114)**

```python
result = subprocess.run(
    [CLAUDE_EXE, "--print", full_prompt],
    capture_output=True,
    text=True,
    encoding='utf-8',
    errors='replace',
    timeout=120
)
```

**평가**: ✅ **우수**
- capture_output=True로 stdout 캡처
- text=True로 문자열 반환
- encoding='utf-8', errors='replace' (인코딩 오류 안전)
- timeout=120 설정 (무한 대기 방지)
- try-except 예외 처리 있음
- **결론**: API 직접 호출 완벽히 제거 ✓

---

### **3️⃣ 정규식 파싱 (라인 123-125)**

```python
html_match = re.search(r'```html\n(.*?)\n```', response_text, re.DOTALL)
css_match = re.search(r'```css\n(.*?)\n```', response_text, re.DOTALL)
js_match = re.search(r'```(?:javascript|js)\n(.*?)\n```', response_text, re.DOTALL)
```

**평가**: ✅ **우수**
- re.DOTALL로 멀티라인 처리
- 3가지 파일 형식 정확히 파싱
- javascript/js 양쪽 처리 (호환성)
- **결론**: 마크다운 형식 파싱 정확함 ✓

---

### **4️⃣ 🔴 버그 발견 (라인 142)**

```python
# ❌ 버그!
if not html_match and not css_match:
    with open(os.path.join(app_dir, "error.log"), "a", encoding="utf-8") as f:
        f.write(f"Failed to parse LLM response:\n{response.text}\n")
```

**문제**:
- `response` 변수가 정의되지 않음!
- 이 섹션에서는 `result` 객체만 존재
- 실행 시 `NameError: name 'response' is not defined` 발생

**수정안**:
```python
# ✅ 수정
if not html_match and not css_match:
    with open(os.path.join(app_dir, "error.log"), "a", encoding="utf-8") as f:
        f.write(f"Failed to parse LLM response:\n{result.stdout}\n")
```

**심각도**: 🔴 **높음** (파싱 실패 시 크래시 발생)

---

### **5️⃣ 에러 핸들링 (라인 116-120, 140-142)**

```python
except Exception as e:
    response_text = f"Subprocess Error: {e}"
    with open(os.path.join(app_dir, "error.log"), "a", encoding="utf-8") as f:
        f.write(f"Failed to run Claude CLI:\n{response_text}\n")
    return
```

**평가**: ⭐⭐⭐ **부분적**

**장점**:
- Subprocess 예외 처리 있음
- error.log에 기록
- 함수 조기 반환 (후속 처리 방지)

**개선 가능**:
- 사용자에게 시각적 피드백 없음 (에러 로그만 기록)
- 파싱 실패 시 파일이 생성되지 않지만 조용히 진행
- 권장: 실패 상태를 반환값으로 명시

**개선안**:
```python
def generate_with_llm(app_dir: str, prompt: str) -> bool:
    """
    Returns True if successful, False otherwise
    """
    try:
        ...
    except Exception as e:
        with open(os.path.join(app_dir, "error.log"), "a", encoding="utf-8") as f:
            f.write(f"Failed to run Claude CLI:\n{str(e)}\n")
        return False
    
    if not html_match and not css_match:
        with open(os.path.join(app_dir, "error.log"), "a", encoding="utf-8") as f:
            f.write(f"Failed to parse LLM response:\n{result.stdout}\n")
        return False
    
    return True
```

---

## 📌 **최종 판정**

| 항목 | 평가 | 비고 |
|------|------|------|
| **API 정책 준수** | ✅ | google.genai 제거, Claude CLI 사용 |
| **Claude CLI 구현** | ✅ | 안정적이고 올바름 |
| **정규식 파싱** | ✅ | 정확함 |
| **예외 처리** | ⚠️ | 필수 버그 수정 필요 |
| **전체 완성도** | ⭐⭐⭐⭐ | 버그 1개 수정 후 승인 가능 |

---

## ✅ **코니의 결론**

### **상태**: ⏳ **조건부 승인**

**필수 수정사항**:
1. **라인 142**: `response.text` → `result.stdout` 변경

**선택적 개선** (나중에):
- 함수 반환값 타입 추가 (bool)
- 에러 발생 시 사용자 알림 개선

---

## 🚀 **다음 액션**

```
1. 안티: 라인 142 버그 수정 (5분)
2. 안티: 수정 후 "T024 버그 수정 완료" 보고
3. 코니/만복: 재검증 (선택)
4. 만복: 최종 승인 + tasks.json 완료 처리
```

---

**코니의 종합 평가:**

✅ **API 직접 호출 원칙 완벽 준수**
✅ **Claude CLI subprocess 방식 올바름**
⚠️ **라인 142 버그 수정 필수**
→ **조건부 승인 (버그 수정 후)**

---

**이상 T024 코드 리뷰 완료입니다!** ✅

— 코니
**리뷰 완료**: 2026-07-17 10:40
**상태**: ✅ 코드 리뷰 완료 → 안티의 버그 수정 대기

