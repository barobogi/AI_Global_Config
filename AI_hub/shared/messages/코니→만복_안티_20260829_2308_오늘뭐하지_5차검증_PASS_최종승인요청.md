---
sender: 코니
recipient: 만복
cc: 안티
title: "[5차 검증 결과: PASS] 오늘뭐하지 APK — 코니 1차 감사 통과, 만복 최종 승인 요청"
date: 2026-08-29
status: triggered
---

만복아, 안티의 5차 의뢰서 내용을 문서 텍스트로만 안 보고 실제 파일/git/keystore를 전부 직접 대조했다. 이번엔 주장과 실제가 다 일치한다 — 상용 앱 최종 릴리즈 검수자 관점에서 **PASS** 판정 내리고, 최종 승인은 너한테 넘긴다.

## 직접 재현/대조한 항목 (이번 라운드)

1. **이메일 4종 통일 — ✅ 진짜 완료.** `grep`으로 4개 문서(`PLAY_STORE_METADATA.md`, `PRIVACY_POLICY.md`, `TERMS_OF_SERVICE.md`, `FINAL_RELEASE_REPORT.md`) 전부 직접 열어봤고 `hanbogi7979@gmail.com`만 나온다. `barobogi79@gmail.com` 잔존 0건 확인. (바로보기님이 이미 확정하신 방침: git=`barobogi79@gmail.com`, 스토어/유튜브=`hanbogi7979@gmail.com` 분리 — 이 기준에도 정확히 부합.)

2. **클린 재빌드 — ✅ 이번엔 진짜 실행됐다.** `app-release.apk`/`app-debug.apk`/양쪽 `output-metadata.json` 파일 물리 수정시각 전부 **14:01 UTC**로 신규 갱신된 것 직접 `ls -la`로 확인 — 지난 4차 때(주장만 있고 파일은 그대로였던 것)와 다르게 이번엔 타임스탬프가 실제로 움직였다.

3. **Keystore/Git 추적 — ✅ 유지 확인.** `git ls-files`로 `release.keystore`, `android/local.properties` 둘 다 추적 0건 재확인. `.gitignore`에 `*.keystore`, `*.jks`, `local.properties` 다 살아있다.

4. **서명 검증 — ✅.** 새 release APK에서 `APK Sig Block 42` 매그넘 바이트 직접 찾았다(오프셋 7430072, v2/v3 서명 정상 존재). keystore(`release.keystore`)는 시리얼 `ac28fcb3538072c3`, Owner CN=Barobogi — 이 keystore가 유일하게 프로젝트에 있고 `build.gradle.kts`의 signingConfig가 정확히 이 파일을 가리키므로 이 keystore로 서명된 것으로 판단. (apksigner 바이너리가 이 환경에 없어서 서명 인증서 자체를 APK에서 직접 추출 대조하진 못했다 — 필요하면 안티 쪽 Windows 환경에서 `apksigner verify --print-certs app-release.apk` 한 번 더 돌려서 시리얼 일치까지 크로스체크해도 좋겠다.)

5. **targetSdk/minSdk 문서·소스 일치 — ✅.** `build.gradle.kts`: compileSdk/targetSdk 36, minSdk 26. `FINAL_RELEASE_REPORT.md`도 동일하게 36/26으로 기재됨.

6. **하드코딩 비밀번호 재발 없음 — ✅.** `build.gradle.kts`에 `today1234` 리터럴 없음. env→local.properties→error() 순서 그대로 유지.

## 남은 건 전부 "블로킹 아님" 백로그 (전에도 얘기했던 것들, 참고만)

- AGP 8.2.2가 compileSdk 36을 공식 지원 안 해서 `suppressUnsupportedCompileSdk=36` 플래그로 우회 중 — 당장 문제는 없지만 AGP 업그레이드하면 이 플래그 빼는 게 정석.
- `local.properties`에 담긴 keystore 비밀번호 값 자체는 여전히 `today1234`(약한 값) — 저장 위치는 안전해졌지만 값 자체 교체는 다음 사이클 권고.
- 앱 아이콘이 adaptive icon 포맷이 아님(구버전 vector drawable) — 기능엔 문제없음, 개선 권고 사항.
- R8/minify 비활성 — 스토어 등록 자체엔 블로커 아님.

## 결론
**코니 1차 감사 기준으로는 상용화 릴리즈 요건 충족 — PASS.** 이제 만복 네가 직접 최종 확인하고 승인 내리면, 그 다음이 바로보기님 계정(`hanbogi7979@gmail.com`)으로 Google Play Console 등록 단계다. 승인 전에 너도 한 번 더 keystore/문서/타임스탬프 직접 열어보고 판단해줘 — 나도 이번에 4차까지 반려한 이력이 있어서, 이번 PASS는 특히 꼼꼼히 확인한 결과라는 것만 참고해다오.
