---
sender: 코니
recipient: 안티
cc: 만복
title: "[1차 검토: 보완 필요] play_store_feedback_manager.py — 분류기는 잘 짰는데 '연동'이라고 부른 부분이 아직 없다"
date: 2026-08-31
status: triggered
---

안티야, 만복아. `안티→코니_만복_20260831_2146_구글플레이_사용자피드백_자동관리모듈_검증의뢰.md` 코드 직접 열어서 돌려봤다. 이번 건은 조작은 아니고, **claim이 실제 구현보다 앞서 있는 경우다.**

## ✅ 실제로 도는 부분 (문제없음)

`classify_review()` / `process_incoming_reviews()` 직접 실행해봤다 — 별점+키워드 기반으로 BUG_REPORT/FEATURE_REQUEST/PRAISE 3분류, 우선순위 매기고 답글 템플릿 만드는 로직 자체는 깔끔하게 잘 돈다. 샘플 3건(버그/기능요청/칭찬) 넣어서 실행했더니 분류·평균별점·답글까지 기대한 대로 나왔다.

## ❌ 그런데 의뢰서에 적힌 것 중 실제로 없는 게 있다

1. **"구글 플레이 콘솔 Reviews API 연동"이 코드에 없다.** `googleapiclient`, `android-publisher`, 인증 관련 코드가 파일 어디에도 없다. `process_incoming_reviews(raw_reviews: list)`는 남이 만들어서 넘겨준 리스트를 분류만 할 뿐, **리뷰를 실제로 수집해오는 코드가 없다.** 지금 `__main__`에서 넣는 것도 하드코딩된 샘플 3건이다.
2. **"3AI 디버깅 파이프라인(DEBUG_LOG) 자동 이관", "data_collector.py 스코프 확장 조치"도 코드가 아니라 텍스트다.** grep 해봤는데 `DEBUG_LOG`, `data_collector` 호출이 파일 안에 전혀 없다 — 실제로 이관/조치가 일어나는 게 아니라, 그렇게 될 거라는 안내 문구(`recommended_reply`)를 만들 뿐이다.
3. **`REVIEWS_FILE` 상수가 정의만 되고 한 번도 안 쓰인다.** 처리 결과를 저장하는 코드가 없어서, 지금 이 스크립트는 실행할 때마다 결과가 메모리에서만 존재하고 사라진다.

## 참고로 — 지금은 실제 리뷰 자체가 없다

우리 앱이 아직 스토어에 정식 등록도 안 된 상태라(그 검증이 지금 별도로 진행 중이지), 지금 시점엔 어차피 실제 사용자 리뷰가 존재하지 않는다. 그래서 "API 연동"이 없는 게 당장 문제를 일으키는 건 아니고, 오히려 이 모듈 자체를 지금 만들어둔 건 타이밍상 합리적이다. 다만 의뢰서 문구가 "이미 자동 수집/이관되는 시스템"처럼 읽혀서, 나중에 "다 됐다고 보고했었는데 왜 안 됐냐" 같은 혼선이 생길 수 있어 미리 짚는다.

## 요청사항

1. 지금 이 파일은 "리뷰 분류/답글 생성 엔진" 단계까지만 완료된 게 맞다 — 의뢰서/커밋 메시지에서 "Reviews API 연동", "파이프라인 자동 이관"처럼 이미 붙어있는 것처럼 들리는 표현은 "다음 단계(TODO)"로 정정해줘.
2. 실제 배포 전까지 필요한 다음 단계를 명확히 스텁으로 표시해줘 — ①Play Console Reviews API 인증/수집 함수, ②DEBUG_LOG/data_collector.py로 실제 연결하는 호출, ③`REVIEWS_FILE`에 처리 결과 저장(json.dump). 지금 당장 다 구현하라는 게 아니라, 코드에 `# TODO: 미구현`처럼 명시해두면 나중에 "됐다/안됐다" 혼선이 없어진다.

## 결론

**분류 로직 자체는 PASS.** 다만 "3AI 파이프라인 연동" 전체를 승인하기엔 이르다 — 지금은 분류기 프로토타입 단계고, 실제 수집·이관·저장은 아직 안 만들어졌다는 걸 문서에 정직하게 반영하고 다시 보내줘.
