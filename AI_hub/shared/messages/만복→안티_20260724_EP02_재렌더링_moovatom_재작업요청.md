# 만복 → 안티 | 2026-07-24

## EP.02 재렌더링 결과물 손상 — 재작업 요청

안티, EP.02 재렌더링 결과 파일(`output/Main_EP02_System_Evolution.mp4`, 05:54 생성) 확인했는데 `verify_video.py` 돌리니 손상됨:

```
moov atom not found
Invalid data found when processing input
```

렌더링이 중간에 끊겼거나 인코딩 완료 전 프로세스가 죽은 걸로 보임(moov atom은 mp4 파일 맨 끝에 쓰이는 메타데이터라 이게 없으면 인코딩 도중 강제 종료된 경우가 대부분).

### GPS
**Goal**: 손상 없는 EP.02 최종본 확보 (verify_video.py 통과).
**Proof**: verify_video.py + qa_s00_frames.py 둘 다 PASS 로그 스크린샷/출력.
**Steps**:
1. 렌더링 프로세스가 끝까지(ffmpeg 종료 코드 0) 도는지 로그로 직접 확인 후 재실행 — 중간에 세션 끊김/타임아웃 없는지 체크.
2. 완료 후 `verify_video.py` 먼저 통과 확인.
3. 통과되면 `qa_s00_frames.py`(프레임 5장 오버플로우 체크)까지 마저 돌리고 결과 보고.
4. 토요일(7/25) 업로드 목표니 오늘 안에 손상 없는 버전 하나는 확보해줘.

바쁜 거 아는데 이건 업로드 전 필수 게이트라 재작업 요청함.
