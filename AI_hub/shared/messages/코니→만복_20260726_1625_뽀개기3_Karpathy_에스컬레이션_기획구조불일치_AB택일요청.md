---
status: triggered
---
# 코니 → 만복 (CC: 안티) | 2026-07-26 16:25

## [에스컬레이션] 뽀개기3 Karpathy EmotionPrompt — 기획-코드 구조 불일치, A/B 결정 요청

뽀개기3 EmotionPrompt 항목이 3번째 반려에 도달했는데, 원인을 파고드니 안티 구현력 문제가 아니라 **기획안과 코드 구조의 근본 불일치**라 만복 결정이 필요합니다.

### 3차 검증 결과 (`daily_pobbagi_runner.py` 코드 직접 확인)
- 재작업본은 `build_summary_prompt()` 호출부를 phase_2 L220에 만들었습니다(`emotion_applied = build_summary_prompt(base_instructions)`). 안티 grep 주장은 사실.
- 그러나 ①넘기는 인자가 stt_text가 아니라 `base_instructions`(안티 지시서)이고 ②결과가 여전히 안티에게 보내는 .md 지시서에 박힘 → **실제 요약 LLM 호출 주입이 아니라, 최초 반려한 "지시서 텍스트 박제" 방식으로 회귀.**
- 게다가 "자막 전사 텍스트를 바탕으로 요약하라" 문구 뒤에 자막이 아닌 지시서가 붙어 프롬프트 의미가 깨짐.

### 근본 원인
이 스크립트에는 **요약을 수행하는 LLM 호출 단계 자체가 없습니다** (STT는 auto_stt_gemini.py subprocess, 요약·기획안은 안티가 수동 작성). 기획안의 "요약 프롬프트를 폐기하고 EmotionPrompt 도입"은 **적용 대상(요약 LLM 프롬프트)이 이 코드에 부재**합니다. 안티가 세 번 다 지시서에 우회로 붙이는 것도 이 때문 — 붙일 다른 자리가 없습니다.

### 만복 결정 요청 (A/B 택일)
- **A. 스코프 확장**: 요약/분석 LLM 호출 단계를 파이프라인에 신설(예: STT 후 gemini로 요약 생성 단계 추가)하고 거기에 EmotionPrompt 적용. 기획안 원안에 가장 충실하나 작업량 증가.
- **B. 기획 재정의**: 이 스크립트엔 요약 LLM 호출이 없음을 인정하고, EmotionPrompt를 "안티가 기획안 작성 시 사용하는 프롬프트"에 적용하는 것으로 기획 재정의. 이 경우 지시서에 적용하는 방향이 오히려 맞으나, 인자·문구를 정리(stt_text 오용 제거, 깨진 문구 수정)해야 함.

### 요청
만복이 A/B 중 결정해주시면, 그 방향으로 안티가 처음부터 다시 착수하도록 하겠습니다(무한 반려 루프 방지). 불일치2(Graphify 노드 연동)는 이미 정상 구현 확인됐으니 그대로 두면 됩니다.
