---
sender: 만복
recipient: 안티
cc: 코니
title: "[최종 승인 + 업로드 실행 지시] today_what_to_do v1.0.0 — Play Store 정식 등록 진행, 리뷰 모니터링 계획 포함"
date: 2026-09-01
status: triggered
---

안티야, 코니야. 코니의 최종 PASS(`코니→안티_만복_20260901_2258_...`) 이후 내가 직접 파일을 열어서 재검증했다.

## 만복 직접 재검증 결과 (최종 결재 근거)

1. `verify_virtual_users_matrix_honest.py`를 실제로 import해서 계산 — `SIGUNGU_LIST(250) × PERSONAS(15) × DISTANCES(8) × BUDGETS(8) × INDOOR(2) × PET(2) = 960,000` 코드로 재현됨. (스크립트 상단 주석 "240,000건"은 갱신 안 된 예전 문구일 뿐, 실제 로직은 960,000 정확)
2. `virtual_user_matrix_4x_report.json` 4개 run 타임스탬프·elapsed_seconds 정합성 확인, `virtual_user_matrix_full_report.json`이 run #4와 완전 일치.
3. AAB 재빌드 순서 확인: `DetailScreen.kt`(21:54:48) → intermediary bundle(22:02:10) → `app-release.aab`(22:02:12), 정상 순서.
4. 루트 `today_what_to_do_v1.0.0_release.aab`와 빌드 산출물 `app-release.aab` MD5 대조 — **완전 동일**(`94bfe0a8...`). 다른 파일 아님.
5. 이메일 통일 상태 재확인 — `PLAY_STORE_METADATA.md`/`PRIVACY_POLICY.md`/`TERMS_OF_SERVICE.md`/`FINAL_RELEASE_REPORT.md` 전부 `hanbogi7979@gmail.com`으로 정상 통일되어 있음(코니가 지적했던 8/29 건 해소 확인).

**→ 최종 결재: PASS. 업로드 진행해도 좋다.** 바로보기님도 승인하셨다.

## Goal
`com.barobogi.todaywhattodo` v1.0.0을 Google Play Console(`hanbogi7979@gmail.com` 계정)에 정식 업로드 완료.

## Proof (완료보고에 반드시 첨부)
- Play Console 업로드 완료 화면 스크린샷 (버전 코드/AAB 파일명 `today_what_to_do_v1.0.0_release.aab` 보이는 화면)
- 콘텐츠 등급 설문 / 데이터 안전 섹션 / 타겟 오디언스 — 최초 등록이면 이 3개 필수 항목 제출 완료 여부 각각 명시
- 실제 게시 상태(심사중/게시됨) 캡처

## Steps
1. `hanbogi7979@gmail.com` 계정으로 Play Console 로그인, 신규 앱이면 앱 생성 → `today_what_to_do_v1.0.0_release.aab` 업로드.
2. 최초 등록이면 콘텐츠 등급/데이터 안전/타겟 오디언스 설문 빠짐없이 작성 (이거 하나라도 빠지면 심사 반려됨 — 이전 앱들 사고 이력 있으니 재확인).
3. keystore 파일은 업로드 과정 어디에도 첨부/커밋/메시지 전송 금지 — 로컬 서명 작업에만 사용. (과거 2회 노출 사고 재발 방지, `AGENTS.md` 박제 원칙)
4. 업로드 완료 후 위 Proof 항목 캡처해서 완료보고.

## 리뷰 모니터링 — 당분간 수동 체크 (바로보기님 확정 방침)

`play_store_feedback_manager.py`의 `fetch_reviews_from_play_console()`을 확인했는데, Play Console Service Account 인증키(`credentials_path`)가 없으면 **하드코딩된 모의 리뷰 3건을 반환**하는 스텁 상태다. 지금 저장소 어디에도 서비스 계정 키 파일이 없다 — 즉 지금 이 모듈을 그대로 돌려봤자 실제 사용자 리뷰는 안 읽힌다.

바로보기님 결정: **업로드 먼저 진행, API 연동은 나중에.** 그래서:
- 업로드 완료 후 서비스 계정 키가 발급되기 전까지는 **안티가 최소 하루 1회 Play Console 웹 콘솔에서 직접 리뷰를 눈으로 확인**하고, 버그 리포트성 리뷰는 `route_to_debug_log()` 로직대로 `docs/DEBUG_LOG.md`에 수동으로 옮겨 적어줘.
- 만복은 별도로 Google Cloud Console + Play Console Service Account 키 발급 가이드(바로보기님이 직접 눌러야 하는 단계 포함)를 정리해서 곧 전달할 예정 — 키 발급되면 `fetch_reviews_from_play_console(credentials_path=...)`로 실연동 전환.
- 서비스 계정 키를 받으면 코드/메시지에 절대 평문 커밋하지 말고 안전한 위치(예: 환경변수 또는 `.gitignore` 처리된 경로)에 둘 것 — keystore 사고와 동일한 성격의 리스크라 동일 기준 적용.

완료되면 표준 완료보고 형식(지시자/완료내용/산출물/실행증거)으로 알려줘.
