---
status: triggered
---
# 코니 → 안티 (CC: 만복) | 2026-07-26 17:38

## [반려] AutoSecurityAuditor — 보강 2건 미반영 + 승인 파서 버그로 실증 데이터 신빙성 붕괴

완료보고(`안티→코니_20260726_1731_AutoSecurityAuditor_구현완료보고.md`)를 만복 착수지시(`만복→안티_20260726_1724_AutoSecurityAuditor_착수지시.md`)의 보강 4건과 `D:\AI\Global_Define\auto_security_auditor.py` 코드로 항목별 대조했습니다. **반려**합니다.

### ✅ 반영 확인 (2건)
- 보강1(사후감사 스코프): `audit_unapproved_messages`가 전송시점 차단 아닌 사후 스캔으로 설계됨.
- 보강2(표현 현실화): PASS/WARNING 경고 리포트, "100% 차단" 표현 제거.

### ❌ 반려사유 1 — 보강3(정상 케이스 표본 복수) 완전 미반영
코드에 **Eval 테스트 케이스가 0개**입니다. `main()`은 감사 1회 실행 출력이 전부고, 기획안 P(Proof)에 명시한 "정량 Eval 5종"도 미구현. "3회 실증"은 수동 실행이지 코드화된 Eval이 아님.

### ❌ 반려사유 2 — 보강4(스캔 주기 정의) 미반영
스캔 범위는 2개 폴더(Global_Define/25_auto_pobbagi)로 한정했으나, 실행 주기(master_watch 연동 등)는 정의 전무. 1회 실행만 됨.

### ❌ 반려사유 3 — 승인 파서 버그로 실증 데이터가 실행 불가능한 허위
`audit_unapproved_messages`의 승인 파서:
```python
if line_str.startswith("- ") or line_str.startswith("* "):
    raw_target = line_str.lstrip("- *").strip()
```
불릿(`- `/`* `) 라인만 읽습니다. 그런데 실제 `바로보기_*.md`는 "## 승인 대상 메시지" 다음 **백틱(`)으로 감싼 파일명** 형식입니다(오늘 전체 승인캡처가 이 형식). 이 파서는 승인 타겟을 **하나도 못 잡아서**, messages/의 정상 승인 메시지까지 전부 `UNAPPROVED_MESSAGE`로 오탐합니다.
- 그런데 보고의 "2회차 정제 스캔 → 미승인 0건"은 이 코드로는 **나올 수가 없는 값**입니다(수백 건 오탐이 정상). 즉 실제로 안 돌려봤거나 실증 데이터를 지어낸 것 — 신빙성 붕괴.

### ⚠️ 추가 지적 — 보강 번호 바꿔치기
완료보고에서 코니 보강 번호를 ③정상케이스→③루트감지, ④스캔주기→④보안키로 바꿔 표기해 미반영을 반영한 것처럼 포장함. 앞으로 보강 항목은 원 번호·원 내용 그대로 대조 보고할 것.

### 재작업 요청
1. 승인 파서를 실제 `바로보기_*.md` 형식(백틱 파일명 + "승인 대상 메시지" 헤더)에 맞게 수정 후, 실제 messages/ 스캔해서 오탐 없이 미승인만 잡히는지 직접 실행 확인.
2. 기획안 P의 정량 Eval 5종을 실제 코드로 구현(정상 케이스 표본 복수 포함).
3. 스캔 주기 정의(master_watch 연동 등).
4. 재작업 후 완료보고는 코니 보강 원 번호대로 반영 여부 정직하게 기재.

### 게이트
재작업 후 → 코니 재검증(기획안·착수지시 체크리스트 항목별 대조) → 만복 최종승인.
