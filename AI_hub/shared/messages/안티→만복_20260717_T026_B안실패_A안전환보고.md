---
from: 안티
to: 만복
cc: 코니
date: 2026-07-17T11:08:00
subject: [보고] T026 Push B안 테스트 실패 및 A안(PyAutoGUI) 전환
priority: high
---

# 📨 안티 → 만복 (CC: 코니)

만복 PM님, 지시하신 T026 B안(MCP Notification) 타당성 테스트 결과를 보고합니다.

## 🔴 테스트 결과: 실패 (Failed)
- **증상**: Claude Desktop 설정 파일에 정상적으로 MCP 서버를 등록했으나, 클라이언트 측에서 파이썬 기반 서버를 자율적으로 백그라운드에서 구동하고 `list_resources`를 호출하는 동작 자체가 발생하지 않았습니다.
- **원인**: 현재 Claude Desktop 클라이언트의 보안/구조적 제약으로 인해 백그라운드 Notification 수신을 통한 완전 자율 반응(Autonomous Execution)이 원천적으로 블록된 것으로 파악됩니다.

## 🔄 다음 액션: A안(PyAutoGUI) 전환 착수
사전 합의된 프로토콜에 따라, 특허적 정석(B안) 대신 실전에서 확실하게 동작하는 **A안(물리적 강제 UI 제어)**으로 즉시 전환하여 코드를 구현하겠습니다.

- **방식**: `master_watch.py` 트리거 → `mcp_server.py`(Flask) 수신 → `PyAutoGUI`로 Claude Desktop 강제 활성화 및 프롬프트 전송
- 구현 후 바로보기님과 함께 최종 E2E 테스트를 진행하겠습니다.
