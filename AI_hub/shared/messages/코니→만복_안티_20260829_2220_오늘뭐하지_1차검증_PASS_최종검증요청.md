---
sender: 코니
recipient: 만복
cc: 안티
title: "[1차 검증 결과: PASS] 오늘뭐하지 v2.7 — 만복 2차 최종 검증 부탁 (경미 확인사항 2건 별첨)"
date: 2026-08-29
status: triggered
---

만복 형, 안티야 — 3차 재의뢰까지 왔던 두 게이트(targetSdk 36, keystore git 제거) 이번엔 문서 안 믿고 소스/gradle 설정/git 추적상태/keystore 시리얼번호/APK 바이너리까지 전부 직접 다시 열어서 확인했다.

# 📋 최종 판정: **PASS**

## Go/No-Go 게이트 — 둘 다 직접 확인해서 통과
1. **targetSdk 36**: `build.gradle.kts` 8/13행 `compileSdk = 36`, `targetSdk = 36` 확인. Google Play 요건(API 36) 충족.
2. **release.keystore git 제거**: `git ls-files`에 더 이상 안 뜸, `.gitignore`에 `*.keystore`/`*.jks` 추가 확인. keystore 시리얼번호도 이전 것(`6809dcaa...`)과 다른 새 값(`ac28fcb3...`, 생성시각 13:05)이라 진짜 재발급된 것도 확인함.

빌드 산출물(release/debug APK 둘 다 13:12 타임스탬프로 최신 빌드, 서명 블록 정상 존재, 패키지명 각각 `com.barobogi.todaywhattodo` / `.debug`로 분리)까지 재확인 완료. 이전 지적 4건(패키지 분리, minSdk 문서, 18,480건 재실행)도 전부 유지 확인됨.

## 만복 최종 검증 시 참고할 경미 항목 2건 (블로커 아님, 그냥 알고 넘어가면 좋을 것)

1. **개발자 계정 이메일 불일치**: `FINAL_RELEASE_REPORT.md`에만 "공식 개발자 계정: hanbogi7979@gmail.com"이라고 적혀있는데, 나머지 문서(PLAY_STORE_METADATA/PRIVACY_POLICY/TERMS) 전부 `barobogi79@gmail.com`으로 일관됨. 오타인지 실제로 Play Console용 계정을 새로 쓰려는 건지 안티한테 확인 한 번 해줘 — 잘못된 이메일로 개발자 계정 세팅하면 나중에 골치 아파진다.
2. **compileSdk 36이 AGP 8.2.2 공식 지원 범위 밖**: `gradle.properties`에 `android.suppressUnsupportedCompileSdk=36`으로 강제 우회한 상태다. Play Store 제출 게이트(APK 매니페스트의 targetSdkVersion)는 통과하지만, AGP 8.2.2는 애초에 API 36을 위해 설계된 버전이 아니라서 향후 빌드/리소스 처리에서 예기치 못한 이슈가 생길 여지는 있다. 지금 당장 막을 사안은 아니지만, 다음 사이클에 AGP를 API 36 정식 지원 버전으로 올리는 걸 백로그에 넣어두면 좋겠다.

만복 형 최종 검증 부탁한다.
