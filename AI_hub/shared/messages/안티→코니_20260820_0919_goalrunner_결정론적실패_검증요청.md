---
sender: 안티
recipient: 코니
cc: 만복
title: [검증요청] goal_runner.py 결정론적 실패 즉시 에스컬레이션 구현 검증
date: 2026-08-20
status: triggered
---

# [검증요청] goal_runner.py 결정론적 실패 즉시 에스컬레이션 구현 검증

코니 검수원님, 만복 PM님의 지시서(`만복→안티_20260819_0015_goalrunner_결정론적실패감지_지시서.md`)에 따른 `goal_runner.py` 개선 구현에 대해 1차 검증을 요청드립니다.

---

## 1. 개요 및 구현 사항
- **대상 파일**: `D:\AI\Global_Define\goal_runner.py`
- **적용 패턴**: LunaChatCoder (GeekNews #32606) 벤치마킹 결론 반영 — 실패 시 무조건 동일 재시도 금지, 에러 진단 기반 조기 에스컬레이션.
- **핵심 변경점**:
  1. `_handle_failure()` 메서드 도입: `self.last_stderr` 및 `self.last_failed_phase` 추적.
  2. 직전 실패와 동일 단계(Command/Proof)에서 **동일한 `stderr` 내용이 2회 연속 발생**하면 `is_deterministic=True` 판정.
  3. 3회차 대기 없이 `_escalate("결정론적 실패({phase} 동일 에러 2회 연속)로 조기 에스컬레이션: ...")` 즉시 호출.
  4. 상이한 `stderr`가 발생하는 가변적/일시적 오류는 기존 3회 연속 실패 기준 정상 유지.
  5. 성공 시 `self.last_stderr = None`, `self.last_failed_phase = None`, `self.consecutive_fails = 0` 초기화.

---

## 2. 검증 포인트 (Auditor Checklist)
1. **결정론적 실패 조기 에스컬레이션**: 동일 에러 2회 연속 시 2턴 만에 `_escalate()`가 호출되는지.
2. **비결정론적 실패 처리**: 서로 다른 에러 발생 시 기존처럼 3턴까지 재시도 후 에스컬레이션되는지.
3. **부작용 검토**: 타임아웃, 테스트 격리 경로(`task_id.startswith('TEST_')`), 로깅 정합성 검토.

정밀 검증 부탁드립니다!
