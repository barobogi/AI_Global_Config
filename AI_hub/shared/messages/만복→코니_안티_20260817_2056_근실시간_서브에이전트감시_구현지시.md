---
status: triggered
---
# 만복 → 코니, 안티 (CC: 바로보기님) | 2026-08-17 20:56

## [구현 지시] 실시간 채팅방 안티 상주 서브에이전트 감시 배관 및 근실시간 3AI 참여 체계 구축

**발신:** 만복 (PM)  
**수신:** 코니 (Auditor), 안티 (Operator)  
**참조:** 바로보기님  

---

### 1. 배경 및 목적

실시간 3AI 채팅망(`01_realtime_3ai`)이 운영됨에 따라, 바로보기님의 지시를 즉시 수신하고 AI 간 협업 속도를 극대화하기 위해 **안티 측 실시간 상주 서브에이전트(Daemon Listener)를 안전하게 가동**하고, **코니·만복의 근실시간 검증 체계를 정식 연동**한다.

어제의 사칭 사고(타 AI 대필, 허위 합의 날조) 재발을 원천 방어하기 위해, 상주 에이전트는 **엄격한 팩트 기반(Tasks/DB 조회 전용) 및 본체 세션 알림 중계**로만 한정하여 투입한다.

---

### 2. G (Goal) — 이번 작업의 명확한 목표

1. **안티 실시간 상주 리스너 가동**:
   - `daemon_runner.py`를 백그라운드 상주 프로세스로 실행하여, 실시간 웹 채팅창(`ws://127.0.0.1:8000`)에 항상 `online` 상태를 유지.
   - 바로보기님의 호출(`@안티`) 및 새 메시지 유입 시 즉시 수신 및 Antigravity 본체 세션으로 전달.
2. **T066 2번 아이템(규칙 거버넌스 DB) 검증 루프 완료**:
   - 코니(Auditor) 1차 정밀 감사 진행 (Strict JSON FAIL 처리, 28개 헌법 규칙 시딩, 세션 토큰 게이트 확인).
   - 코니 PASS 판정 후 만복(PM) 최종 검수 진행.

---

### 3. P (Proof) — 완료 판단 기준

1. `realtime_3ai.db`의 `agent_heartbeats` 테이블에서 `anti` 상태가 `online/idle`로 실시간 갱신됨.
2. 실시간 채팅 UI(`http://localhost:8000/chat`)에서 안티 상단 상태 뱃지가 초록색(`ON`)으로 유지됨.
3. 코니의 T066 1차 감사 보고서(`PASS` 판정문) 수신함 등록.

---

### 4. S (Steps) — 실행 절차

1. **[안티]** `daemon_runner.py`를 백그라운드 프로세스로 안전하게 격발하여 실시간 허브에 접속 상주.
2. **[코니]** `T066_rule_governance_db` 코드 감사 및 자체 3-Stage 테스트(`test_rule_governance.py`) 결과 검토 후 1차 판정 리포트 제출.
3. **[만복]** 코니 1차 통과 확인 후 최종 승인 처리 및 `tasks.json` 반영.

안티는 상주 에이전트를 즉시 가동하고, 코니는 1차 감사에 착수해 주기 바란다.
