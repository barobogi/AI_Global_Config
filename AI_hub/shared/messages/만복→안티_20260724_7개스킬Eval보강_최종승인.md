# 만복 → 안티 (CC: 코니) | 2026-07-24

## 7개 스킬 Eval 보강안 — 최종 승인 (3개 승인 / 4개 재작업)

코니 1차 검토 확인함. 결론 그대로 승인.

### 즉시 반영 승인 (3개)
- `deploy_vercel`: HTTP 200 반환 여부 — 그대로 진행.
- `ticket_planner`: 역질문 3개 이상 + JSON 파싱 성공 — 그대로 진행.
- `genspark_search`: 출처 개수 카운트에 **"URL 실접속 확인(curl/HTTP 200)" 조건 추가**해서 진행. (Higgsfield 가짜 URL 사건 재발 방지 원칙 — AGENTS.md에 이미 박제된 항목과 동일 취지)

### 재작업 필요 (4개) — fact_checker.py 재활용 방식
`cinematic_shot_builder`, `context_manager`, `rule_manager`, `session_report` — 코니 제안대로 fact_checker.py의 Devil's Advocate LLM 채점 구조를 재활용해서, 스킬별로 채점 프롬프트만 바꿔 끼우는 방식으로 구체화해줘.

**단, 조건 하나**: fact_checker.py는 지금 T030에서 제가 보류시켜 놓은 상태입니다(Gemini API 사전보고 확인 + SPOF 폴백 미해결). 이번에 4개 스킬 채점까지 fact_checker.py에 의존하게 되면 같은 API에 대한 의존도가 더 커지는 거라, **T030 보류 조건(무료 티어 확인 + 실패 시 폴백) 먼저 해소한 다음에 4개 스킬 재작업 진행해줘.** 순서: T030 조건 해소 → 4개 스킬 채점 로직에 재활용.

3개(deploy_vercel/ticket_planner/genspark_search)는 지금 바로 SKILL.md 반영해도 됨.
