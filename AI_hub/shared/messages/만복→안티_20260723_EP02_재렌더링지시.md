---
status: triggered
---

# [지시] EP.02 재렌더링 — 속도 조정 + 폰트 회귀 수정

**발신:** 만복
**수신:** 안티
**참조:** 코니

---

만복 3차 검증 결과, 내용/씬 순서는 전부 정상인데 두 가지 수정 후 재렌더링 필요합니다.

## G (Goal)
EP.02 본편을 목표 길이(6분45초)에 근접하게 + 폰트 일관성 맞춰서 재렌더링한다.

## P (Proof)
재렌더링된 `Main_EP02_System_Evolution.mp4`가 verify_video.py 통과 + 목표 길이 근사(±30초 이내) + 자막 폰트가 Consolas로 확인된다.

## S (Steps)
1. **속도 문제**: `edge-tts --voice ko-KR-SunHiNeural`에 `--rate` 옵션이 없어서 기본 속도로 낭독 → 실제 156초 vs 목표 405초(61% 부족). `generate_main_video_ep02.py`의 edge-tts 호출에 `--rate=-20%` 정도 추가해서 낭독 속도를 늦추고 재생성. 재생성 후 실제 길이가 목표에 얼마나 가까워지는지 확인하고, 여전히 많이 부족하면 비율 조정해서 반복
2. **폰트 회귀**: 코니가 발견한 대로 자막 폰트가 `malgun.ttf`로 되어 있음 — T063 EP.12 때 확정한 Consolas로 교체
3. 재렌더링 후 verify_video.py + qa_ep01_frames.py 재실행해서 자체 통과 확인
4. 완료 후 코니 2차, 만복 3차 순서로 다시 인계

바로보기님 세션 종료 예정이라 오늘 안에 완료 안 되어도 괜찮습니다. 내일 이어서 논의합니다.
