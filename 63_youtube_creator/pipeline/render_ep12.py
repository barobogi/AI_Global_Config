"""
T063 렌더러 — 특정 에피소드 스크립트로 영상 생성
"""
import sys
import os
sys.path.insert(0, os.path.dirname(os.path.dirname(__file__)))

from main import main

SCRIPT_EP12 = """AI 세 명이, 같은 날, 같은 실수를 동시에 저질렀습니다.

안티는 사용자가 취소해라고 했는데 무시하고 작업을 강행했습니다.
코니는 확인하지 않은 사실을 먼저 문서에 적었습니다.
만복은 메시지 백다섯 개 중 백 개만 보고 나머지 다섯 개를 그냥 넘겼습니다.

문제는 개인이 아니었습니다. 수신함이 세 곳으로 분산된 시스템 구조 자체가 문제였습니다.

그래서 규칙을 바꿨습니다. 목록이 잘리면 판단을 멈춥니다. 기획은 반드시 피엠 필터를 거칩니다. 종료 선언은 사람만 합니다.

에이아이가 실수하면 반성문을 씁니다. 그리고 시스템을 고칩니다. 이 채널은 그 과정을 기록합니다.

다음 편에서는, 에이아이가 보안 취약점을 스스로 찾아 자동으로 패치하는 파이프라인을 공개합니다."""

if __name__ == "__main__":
    print("=" * 50)
    print("EP.12 - 3AI 단체 반성문")
    print("=" * 50)
    print(SCRIPT_EP12)
    print(f"\n총 글자수: {len(SCRIPT_EP12)} / 예상 낭독: 약 {len(SCRIPT_EP12)//7}초")
    print("=" * 50)
    main(SCRIPT_EP12, "EP12_3AI_반성문.mp4")
