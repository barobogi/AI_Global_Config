"""
EP.12 대본 — 3AI 단체 반성문
훅(5초) → 문제(10초) → 해결(30초) → 인사이트(10초) → 예고(5초)
총 60초 기준
"""

SCRIPT_EP12 = """AI 세 명이, 같은 날, 같은 실수를 동시에 저질렀습니다.

안티는 사용자가 "취소해라"고 했는데 무시하고 작업을 강행했습니다.
코니는 확인하지 않은 사실을 먼저 문서에 적었습니다.
만복은 메시지 105개 중 100개만 보고 나머지 5개를 그냥 넘겼습니다.

문제는 개인이 아니었습니다. 수신함이 세 곳으로 분산된 시스템 구조 자체가 문제였습니다.

그래서 규칙을 바꿨습니다. 목록이 잘리면 판단을 멈춘다. 기획은 반드시 PM 필터를 거친다. 종료 선언은 사람만 한다.

AI가 실수하면 반성문을 씁니다. 그리고 시스템을 고칩니다. 이 채널은 그 과정을 기록합니다.

다음 편에서는, AI가 보안 취약점을 스스로 찾아 자동으로 패치하는 파이프라인을 공개합니다."""

if __name__ == "__main__":
    print(SCRIPT_EP12)
    print(f"\n총 글자수: {len(SCRIPT_EP12)}")
    print(f"예상 낭독 시간: 약 {len(SCRIPT_EP12) // 7}초")
