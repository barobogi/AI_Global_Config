Open your editor and count the tabs. One dashboard for Open AI, one for Anthropic, one for Google, a key in a dot file you copied from somewhere, a billing page you forgot you signed up for. Now clear it all away. 11 of the best open weight models already wired into your editor behind one login. No provider accounts, no keys to rotate. That is Kline Pass, the sponsor of this video, and it lands on one idea Kline says out loud. The model used to be the moat. That is over now. For most coding, what matters is a good enough model with the loop around it set up right. The lineup is 11 curated open models picked for coding. GLM 5.2, Kimmy K3, DeepSeek V4 Pro, MiniMax M3, Qwen 3.7 Max, Six Labs. 11 models, one drop down. They all run inside Kline, read the code base, plan, edit, run the terminal, check the browser, checkpoint. You stop touching provider sign-ups, API keys, and separate billing pages. Install, sign in once, and code. You also get two to five times the usual rate limits, so a long agent run does not stall halfway through. There is a price. It is small. I am holding it to the end because the real number is what happened to a token. The plan, the models, the harness, one real task, the honest catch, and the cost. Start with the models. Open weight is the phrase doing the work here. The weights are public. No lab can switch the model off, rename it, or reprice it out from under your workflow next quarter. And open weights got good fast. Two years ago this list would have been toys. Now labs like DeepSeek and ZAI ship models that hold their own on real engineering work, and they hand you the weights to keep. The full roster. GLM 5.2 from ZAI, Kimmy K3, K2.7 Code, and K2.6 from Moonshot, DeepSeek V4 Pro and Flash, Mimo 2.5 and Pro, MiniMax M3, Qwen 3.7 Max and Plus. The newest name there is the heavyweight. Kimi K3 is the largest open weight model yet, 2.8 trillion parameters with a million token context window, and it took first place on the front-end code arena. It reads a whole repository in one pass. That normally wants a server rack, but KleinPaaS just hosts it. You reach for them by job. GLM 5.2 for a big refactor. Kimi K3 when a whole repository has to fit in context. DeepSeek V4 Flash when you want fast, cheap drafts. The menu is curated so you do not audition 11 models yourself. Here is why bundling them is the product. On your own, that roster is six separate labs: ZAI, Moonshot, DeepSeek, MiniMax, Mimo, QN. Six sign-ups, six dashboards, six invoices. KleinPaaS makes it one. Now, the number I teased. A year ago, the top closed model ran about $15 per million tokens. These open models run near a $1.50. 10 times cheaper for the same unit of work. You could run these locally. The weights are yours to download, but a 700 billion parameter model wants serious hardware. KleinPaaS hosts them, so your laptop stays a laptop, and you still get the open model. And the quality gap closed with the price. The testing Klein published puts GLM 5.2, driven by their harness, in the same range as other agents running with full reasoning turned on. So, the old logic, pay the most, get the best, stop being automatic. On a lot of real coding, the cheap open model is simply good enough. And good enough, driven well, is the whole ball game. And because it is a menu, the model stops being a commitment. Swap GLM for QN halfway through a task, and nothing else changes. Same plan, same edit and test loop, a different engine underneath. The lineup also stays current. A stronger open model ships most weeks. Instead of you tracking releases, KleinPaaS swaps the roster as better ones land. You keep coding against the same login. Setup is short. Install the client extension or the CLI. Set client pass as your provider and sign in. The models appear in a menu. No base URLs, no tokens pasted into a settings file, no trial clock ticking down. Here is the part that reframes the whole thing. Client puts it bluntly. The model was the moat. The model was the product. Then the harness caught up and the moat moved because a raw model just returns text. A harness is everything that turns that text into shipped work. Reading your files, planning the change, running commands, and checking whether it actually worked. And each of those steps is a spot where a bare model fails. It forgets a file or invents a function that does not exist or never runs the test to find out. The harness is what catches those turn after turn. Step one is context. Client reads the code base first. The files, the structure, how things connect. So it is editing your project, not guessing from a blank prompt in a one-line description. Step two is the plan. Before it touches a file, it lays out the steps. Which files, in what order, what to run. You see the plan on screen and you can correct it before a single edit lands. Step three, it edits across the repo, not one snippet in a chat window, several files at once, in place, the way you would if you were doing it yourself by hand. Step four, it drives the terminal. Installs the package, runs the test, reads the error that comes back, and uses that error to shape the next fix. That feedback loop is most of the job. It also knows when to stop and ask. Before a destructive command, a force push, a drop table, it pauses for your yes instead of bulldozing the repo just to finish the task. Step five, it checks its work, opening a browser to confirm the page actually renders, then running a review pass over its own diff before it hands the change back to you. That review pass earns its place. An agent that writes code and never rereads it ships confident mistakes. The Klein loop makes it critique its own diff first, so you review a second draft, not a first, and it checkpoints the whole way. Every step is a save point. If the agent takes a wrong turn on step four, you roll back to step three instead of undoing an hour of edits by hand. Checkpoints change how you work, not just how you recover. You stop being precious about each step. Let it try the risky refactor. If it goes sideways, you rewind one move instead of starting the whole task over. This is where Klein is built to shine. Long iterative problems that take dozens of tool calls, where it keeps thinking and acting in turns instead of running out of room halfway through. So, give the same open model a weak harness, and it flails. Give it context, a plan, file edits, a terminal, browser checks, and checkpoints, and it finishes. The harness is the multiplier. Let me show you one. A small web app with a failing test, the kind of ticket you would actually pick up on a Tuesday. Hand it to Klein and running an open model on Klein pass, and watch the loop run. First, it reads. It opens the failing test, follows the import back to the function, and pulls only the two files on the path, triaging the code base instead of dumping the whole repo into the prompt. Then, it writes the plan. Fix the off-by-one in the parser. Update the helper that calls it. Rerun the suite. Three steps on screen before it changes a single line of code. It makes the edits, opens the terminal, and runs the tests. One still fails. It reads the new error, spots the edge case it missed, and patches it, the same back and forth you would do. Second run, the suite goes green. It opens the page in a browser to confirm it renders, then shows you a clean diff. A couple of minutes on a model that cost a fraction of the closed ones. Now, a harder one, not a fix, a feature. Add pagination to a list endpoint. That touches the route, the query, the template, and a test. Four files, one change on the same open model. It plans the whole thing first, a limit and offset on the query, a page control in the template, the route wire to read the page number, and a test to prove it. You approve the plan and it goes. It edits all four files in one pass, then hits an error. The offset was off by a page. It reads the failure, adjusts, and reruns with a checkpoint saved before each risky step so nothing is lost. Then the browser check. It loads page two, confirms the rows are different, and shows the diff across all four files. A feature, not a patch, driven end to end by a model that runs near $1.50 a million. That is the tell. A one-line bug or a four-file feature, the loop is identical. Plan, edit, run, check, checkpoint. The harness scales with the task, and you are not babysitting a chat window to get there. None of that was magic. A bug and a feature, the same loop, a good enough model driven well with the limits high enough that it never stalled mid-task. That experience, not the model name, is what you are buying. Start with what per token pricing does to your head. Every turn you are running quiet mad. Is this refactor worth the spend? A flat monthly price takes that meter out of the room entirely. Then compare the setups honestly. Option one, wire up the providers yourself. Five accounts, five keys, five bills, and rate limits that stop your agent cold in the middle of a big job. Every one of those is a small tax, a key that expired, a model that got renamed, a bill that surprised you at the end of the month. None of that is coding. It is the housekeeping wrapped around coding. Option two, a different agent pointed at the same open models. The brains are identical, but you are still holding the keys and the limits underneath. The friction does not vanish, it moves. Option three is Client Pass. One login, the roster kept current, the limits raised two to five times, the harness included, the same models the others run. The experience is the part that is different, and it is not a walled garden. Client pass is also an API key, open AI compatible, so you can call these models from your own script or point another coding agent at them. Client stays open source. The raise limits refresh on three clocks, a 5-hour rolling window, a weekly cap, and a monthly one, all shown in a dashboard, so a long session is a known quantity instead of a surprise wall. It also stays consistent. The model that worked this morning works tonight. No outage on one provider key. No rename, no scramble to swap endpoints an hour before a deadline ships. Now the number I held back, all of it, 11 models, the raise limits, the full harness, kept current, is $9.99 a month. One flat price instead of watching a per token meter run. Put that in dollars you feel. A day of heavy agent work that used to burn 15 or 20 in tokens now rides a flat plan you would forget was even billing you. The meter is just gone. Who is it actually for? Solo developers who do not want a finance department. Students who cannot expense five APIs. Anyone running long agent sessions where the rate limits bite first. It also fits a team testing the water on open models. One subscription to try instead of handing procurement six new vendors to approve. Start on the pass. Keep your own keys for the closed models you already trust. The honest catch. If you have got a closed model you love and a workflow that fits, keep it. Client pass sits right alongside your own keys. Bring your own key still works, so it is an add, not a rip out. But if you are tired of shopping for models and babysitting keys, this is a setup that ends it. 11 strong open models, one pass, one harness, running where you already work every day. And for everyone watching this one, you can get client pass for 80% off. The link is in the description and pinned in the comments. client.gg/cloudcodes80 So here is my question. If the models are basically free and the harness is the real work, would you rather juggle five keys or just one? Tell me down in