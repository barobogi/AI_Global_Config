# 상용 경쟁앱 Deep Search + 킬러기능 도입 기획안

> 작성: 만복 (Planner) | 2026-08-29
> 요청 배경: 바로보기님이 1차 APK 실사용 후 "GPS/동행자 세분화는 당연, 트리플·데이트팝·마이리얼트립 등 상용 앱 대비 앱이 단촐해 보인다" 피드백
> 원본 요청: `AI_hub/shared/messages/안티→만복_코니_20260829_오늘뭐하지_상용앱_Deep서치_아이디어_보강_요청.md`

---

## 1. 경쟁앱 5종 Deep Search 요약

| 앱 | 핵심 킬러 기능 | 오늘뭐하지에 줄 시사점 |
|---|---|---|
| **트리플** | AI 일정 생성, 동행자 초대해 함께 계획, 코스 공유/경비 정산, 도시가이드 | 코스를 "혼자 보는 결과"가 아니라 "같이 보고 공유하는 것"으로 |
| **데이트팝** | 위치기반 데이트코스 추천, 실시간 리뷰, 구독형 딜(팝패스) | 동행자=연인일 때 특화된 톤/추천 로직 필요 |
| **캐치테이블** | 조건조합 실시간 검색(지역+편의시설+목적), 빈자리 알림, 웨이팅 미루기 | "지금 가면 바로 되는지" 실시간성이 만족도를 좌우 |
| **핫플가이드** | 스마트어라운드(반경 슬라이더+시간대/성별/음식종류 필터), 실판매량 기반 인기메뉴 순위 | 반경/조건을 결과 화면에서 바로 재조정하는 UX |
| **마이리얼트립** | 과거 이력 기반 초개인화 추천, 빠른 반응형 UI(로딩 1.7초 단축) | 동행자 타입별 가중치 자동 조정 |

**공통 패턴**: 5개 앱 모두 (1) 조건을 결과 화면에서 즉시 재조정 가능, (2) 동행자/상황별로 추천 로직 자체가 달라짐, (3) 공유·소셜 요소가 있음. "오늘뭐하지"는 이 3가지가 약함 — 현재는 조건입력→결과가 1회성이고, 동행자는 "아이 동반 여부" 정도만 반영.

---

## 2. 도입 제안 TOP 5

### ① 동행자 타입별 가중치 프리셋 (마이리얼트립·데이트팝 영감)
- **내용**: 동행자를 "연인/가족(아이동반)/친구/혼자" 4종으로 세분화, 각 타입마다 `score_engine.py`의 `weights` dict를 다르게 적용 (예: 연인→weather_fit·novelty↑, 가족→distance_score·budget_fit↑, 혼자→time_fit↑)
- **기존 로직과의 결합**: `ScoreEngine.__init__`의 `self.weights`를 고정값 대신 `companion_type` 인자 받아 프리셋 딕셔너리에서 선택하도록 확장. 하드필터/장소 데이터 구조는 그대로.
- **난이도**: 중 (백엔드 로직 확장 + Android ConditionInputScreen에 동행자 타입 선택 UI 추가)

### ② 반경 슬라이더 + 결과 화면 즉시 재조정 (핫플가이드 영감)
- **내용**: ResultScreen에 반경(1/3/5/10km) 슬라이더 배치, 조정 시 조건입력 화면으로 안 돌아가고 바로 재필터링
- **기존 로직과의 결합**: `hard_filter.py`의 `check_distance()`가 이미 있어 백엔드 변경 거의 없음 — API에 `max_distance_km` 파라미터로 재호출만 하면 됨
- **난이도**: 하

### ③ 코스 공유 카드 (트리플 영감)
- **내용**: 추천받은 3코스 중 하나를 카카오톡/링크로 공유 가능한 카드 이미지로 생성
- **기존 로직과의 결합**: 완전 신규 기능이지만 백엔드 추천 결과(장소명/이유/지도좌표)는 이미 존재 — 이미지 렌더링(Compose Canvas 또는 이미지 조합) + 카카오 공유 SDK만 추가
- **난이도**: 중

### ④ "지금 가기 좋음" 실시간 배지 (캐치테이블 영감)
- **내용**: 영업 마감 임박(1시간 이내)이면 "곧 마감" 경고 배지, 방금 열었으면 "지금 한산할 가능성" 배지
- **기존 로직과의 결합**: `hard_filter.py`의 `check_is_open()`이 이미 영업시간 판정하고 있어, 마감까지 남은 시간만 추가 계산하면 됨 — 공공데이터라 실시간 혼잡도는 불가하므로 "시간대 기반 추정"으로 명확히 표기
- **난이도**: 하

### ⑤ 인기도 시각 배지 (핫플가이드·데이트팝 영감)
- **내용**: `score_engine.py`의 `popularity`(가중치 0.10) 점수를 ResultScreen 카드에 "이번 주 인기" 같은 배지로 시각화
- **기존 로직과의 결합**: 백엔드 변경 전혀 없음, 이미 계산되는 점수를 UI에 노출만 하면 됨
- **난이도**: 하 (가장 빠르게 적용 가능, 우선순위 1순위 추천)

---

## 3. 구현 우선순위 제안 (안티 검토용)
난이도 하(⑤→②→④) 먼저 적용해 빠르게 "상용급으로 보이는" 체감 개선 → 이후 중(①→③) 순으로 확장.

## 4. 검토 필요 사항 (코니 Auditor 확인 요청)
- ① 동행자 타입별 가중치 수치는 임의 설정값 — 실사용자 데이터 없이 감으로 정한 것이라 UX 관점 타당성 검토 필요
- 3AI 뿌리체계/차별화 관점에서, 경쟁앱 벤치마킹이 "오늘뭐하지"만의 정체성(공공데이터 기반 무료·AI 팩트체크)을 희석시키지 않는지 확인 필요

---

## 5. 출시 전 회피 체크리스트 (사용자 후기 Deep Search 기반)

> 바로보기님 지시로 5개 경쟁앱의 **부정적 후기/이탈 포인트**를 Deep Search해서, "오늘뭐하지"가 똑같이 겪을 위험이 있는지 코드 기준으로 실제 점검함.

| 불만 유형 | 확인된 앱 | 우리 앱 위험도 | 근거 / 대응 방안 |
|---|---|---|---|
| **추천이 뻔함/다양성 부족** | (데이트팝 — 코스 개수 적어서 아쉬움) | **⚠️ 높음 — 실제 코드로 확인된 리스크** | `score_engine.py`의 `generate_recommendations()`가 점수 내림차순 정렬 후 `top_k`만 그대로 뽑아씀(156행), 카테고리 다양성 재랭킹 로직 없음 — 상위권이 특정 카테고리(예: 카페)로 쏠리면 3코스가 비슷해 보일 위험 실재. **대응**: top_k 추출 전에 카테고리별 최소 1개씩 우선 배정하는 간단한 다양성 로직 추가 권장 (score_engine.py 수정 필요, 난이도 하) |
| **정보 커버리지 부족 (신생/비주류 장소 미등록)** | 트리플 ("앱에 없는 정보는 입력 안 됨") | 중 — 구조적으로 존재하나 완화됨 | 우리도 관광공사 공공데이터(`numOfRows=40`, `data_collector.py`)가 소스라 신생 핫플/비제도권 장소는 원천적으로 누락됨. 다만 "공공데이터 기반 신뢰성"이 우리 앱의 핵심 정체성(AI 팩트체크)이라 커버리지보다 신뢰도를 파는 포지셔닝이면 오히려 강점으로 전환 가능. **대응**: 온보딩/소개 문구에 "검증된 공공데이터 기반 — 등록 안 된 신상 핫플은 없을 수 있음"을 미리 명시해 기대치 관리 |
| **속도 저하/버벅임, 서버 장애** | 데이트팝 | 낮음 (현재 스캐폴딩 단계) | 아직 트래픽 없는 MVP라 지금 판단은 이름 — 다만 Render 무료 인스턴스는 유휴 시 슬립(요청 시 50초+ 지연) 되므로, 실사용자 첫 요청에서 "느리다" 불만으로 바로 이어질 수 있음. **대응**: 온보딩 화면에 "첫 요청은 서버 기동으로 최대 1분 걸릴 수 있음" 안내 문구, 또는 유료 인스턴스 전환 시점 사전 논의 필요 |
| **리뷰 신뢰도/조작 의혹** | 캐치테이블 (낮은 별점 리뷰 삭제 의혹) | **없음 — 구조적으로 해당 안 됨** | 오늘뭐하지는 자체 리뷰/평점 시스템이 없고 공공데이터 인기도 지표만 씀 — 이 불만 유형 자체가 발생할 수 없는 구조. 오히려 "리뷰 조작 없는 투명한 추천"으로 마케팅 포인트 삼을 만함 |
| **환불/취소 정책 불투명·분쟁** | 마이리얼트립 (91% 환불 등 분쟁 사례), 캐치테이블 (예약금 취소 미처리) | **없음 — 결제/예약 기능 자체가 없음** | 현재 스코프는 추천만 하고 예약/결제는 안 함 — 해당 리스크 없음. 단, 향후(2탄 등) 예약 연동 기능 추가 시 이 항목 재검토 필수 |
| **핫플가이드** | — | — | 검색 결과 부족 — 앱 인지도가 낮아 리뷰 데이터 자체가 검색되지 않음, 별도 회피 체크리스트 도출 불가 |

**우선 조치 권고**: 위 항목 중 "추천 다양성 부족"이 유일하게 **지금 코드 수준에서 확인된 실질 리스크**임 — TOP 5 도입 제안과 별개로, 출시 전 `score_engine.py`에 카테고리 다양성 로직을 추가하는 걸 최우선 검토 권장.

Sources: [트리플 App Store](https://apps.apple.com/kr/app/%ED%8A%B8%EB%A6%AC%ED%94%8C-%ED%95%AD%EA%B3%B5-%ED%98%B8%ED%85%94-%EC%B5%9C%EC%A0%80%EA%B0%80-%EC%98%88%EC%95%BD-%EC%97%AC%ED%96%89%EA%B3%84%ED%9A%8D/id1225499481) · [데이트팝 리뷰(데일리팝)](https://www.dailypop.kr/news/articleView.html?idxno=79150) · [캐치테이블 리뷰 삭제 의혹(인사이트)](https://www.insight.co.kr/news/513268) · [캐치테이블 예약금 취소(블라인드)](https://www.teamblind.com/kr/post/%EC%BA%90%EC%B9%98%ED%85%8C%EC%9D%B4%EB%B8%94-%EC%98%88%EC%95%BD%EA%B8%88-%EC%B7%A8%EC%86%8C-%EA%BC%AD-%ED%99%95%EC%9D%B8%ED%95%98%EC%84%B8%EC%9A%94-FU1tRwYi) · [마이리얼트립 환불 분쟁(트래블아이)](https://traveli.net/news/view.php?no=10700)
